import { Question } from "../../types.js";

export const genaiFundamentalsQuestions: Question[] = [
  {
    id: "aif-gen1",
    domain: "genai-fundamentals",
    text: "A developer is estimating the monthly bill for a summarization feature that calls a large language model on Amazon Bedrock, where on-demand pricing charges per input token and per output token. What is a token?",
    options: [
      { id: "a", text: "A word or part of a word that the model handles as one unit" },
      { id: "b", text: "A single character of the prompt" },
      { id: "c", text: "One complete request to the model, however long the text is" },
      { id: "d", text: "A numeric vector that captures the meaning of a sentence" },
      { id: "e", text: "A temporary security credential issued by AWS STS" },
      { id: "f", text: "One layer of the model's neural network" },
      { id: "g", text: "An API key that authorizes the Bedrock call" },
      { id: "h", text: "A unit of text from the model's fixed vocabulary" },
    ],
    correctOptionIds: ["a", "h"],
    answerType: "single",
    explanation: "Models break text into tokens, each roughly a word or a fragment of a word, and on-demand Bedrock pricing counts both the tokens sent in the prompt and the tokens generated in the response, so prompt length affects cost as much as response length.",
    optionRationale: {
      a: "A token is the unit of text the model processes, typically a short word or a word fragment.",
      b: "Tokenizers usually group several characters together, so a token is larger than one character.",
      c: "Billing is per token, not per request, which is why long prompts cost more than short ones.",
      d: "A numeric vector that captures meaning is an embedding, a different concept from a token.",
      e: "STS issues credentials for authentication; it has nothing to do with how text is counted.",
      f: "Layers are part of the model architecture and are not what the service meters.",
      g: "An API key or IAM credential authorizes the call; the token count measures the text in it.",
      h: "A model splits text into entries from the fixed vocabulary it was trained with, and each of those entries, typically a word or a piece of a word, is one token.",
    },
    referenceUrl: "https://docs.aws.amazon.com/bedrock/latest/userguide/key-definitions.html",
    referenceLabel: "Amazon Bedrock key terminology",
    consoleUrl: "https://console.aws.amazon.com/bedrock/",
    consoleLabel: "Amazon Bedrock console",
  },
  {
    id: "aif-gen2",
    domain: "genai-fundamentals",
    text: "A team converts each product description into a list of 1,024 floating point numbers, arranged so that descriptions with similar meaning sit close together in that numeric space. What are these numeric representations called?",
    options: [
      { id: "a", text: "Embeddings" },
      { id: "b", text: "Tokens" },
      { id: "c", text: "One-hot encodings" },
      { id: "d", text: "Model weights" },
      { id: "e", text: "Hyperparameters" },
      { id: "f", text: "Model checkpoints" },
      { id: "g", text: "Prompt templates" },
      { id: "h", text: "Dense vectors" },
    ],
    correctOptionIds: ["a", "h"],
    answerType: "single",
    explanation: "An embedding is a dense vector that represents text, an image, or another input so that distance between vectors reflects similarity in meaning; embedding models such as Amazon Titan Text Embeddings and Cohere Embed produce them.",
    optionRationale: {
      a: "Embeddings are exactly this: vectors positioned so that semantically similar inputs are near each other.",
      b: "Tokens are the chunks of text fed to the model, not the vector that represents their meaning.",
      c: "A one-hot encoding is a sparse vector with a single 1 per category and carries no notion of similarity.",
      d: "Model weights are the learned parameters inside the model, not a representation of one input.",
      e: "Hyperparameters are settings chosen before training, such as learning rate or batch size.",
      f: "A checkpoint is a saved copy of a model's state during or after training.",
      g: "A prompt template is reusable text with placeholders, not a numeric representation.",
      h: "A fixed-length list of floating point values positioned by meaning is a dense vector, which is the form a semantic embedding takes.",
    },
    referenceUrl: "https://aws.amazon.com/what-is/embeddings-in-machine-learning/",
    referenceLabel: "What are embeddings in machine learning? (AWS)",
  },
  {
    id: "aif-gen3",
    domain: "genai-fundamentals",
    text: "An analyst pastes a 200-page contract into a single prompt and the model rejects the request because the input is too long. Which model characteristic does this limit describe?",
    options: [
      { id: "a", text: "The context window, which is the maximum number of tokens the model can take in and generate for one request" },
      { id: "b", text: "The temperature setting" },
      { id: "c", text: "The knowledge cutoff date of the training data" },
      { id: "d", text: "The requests-per-minute service quota" },
      { id: "e", text: "The number of parameters in the model" },
      { id: "f", text: "The dimension of the model's embeddings" },
      { id: "g", text: "The top-k value used during sampling" },
      { id: "h", text: "The model's maximum sequence length" },
    ],
    correctOptionIds: ["a", "h"],
    answerType: "single",
    explanation: "The context window, also called the maximum sequence length, is the fixed token budget a model can attend to in one call, covering the prompt and the generated response together; oversized input must be split into smaller pieces or sent to a model with a larger window.",
    optionRationale: {
      a: "Exceeding the token budget for a single request is precisely a context window limit.",
      b: "Temperature changes how randomly tokens are sampled and has no effect on how much text fits.",
      c: "A knowledge cutoff limits what the model knows about, not how much text it can read at once.",
      d: "A requests-per-minute quota throttles how often you can call the model, not the size of one call.",
      e: "Parameter count describes model size; a large model can still have a small context window.",
      f: "Embedding dimension is the length of a vector produced by an embedding model.",
      g: "Top-k restricts which candidate tokens are sampled and does not change the input limit.",
      h: "Maximum sequence length is the most tokens the model can process in one pass, so an oversized contract exceeds it and is rejected.",
    },
    referenceUrl: "https://aws.amazon.com/what-is/large-language-model/",
    referenceLabel: "What is a large language model? (AWS)",
  },
  {
    id: "aif-gen4",
    domain: "genai-fundamentals",
    text: "A bank's case summarizer must produce the most consistent and focused wording it can for the same input, avoiding creative rephrasing. Which inference parameter change best supports that goal?",
    options: [
      { id: "a", text: "Lower the temperature" },
      { id: "b", text: "Raise the temperature" },
      { id: "c", text: "Raise top-p toward 1.0" },
      { id: "d", text: "Raise top-k to a much larger value" },
      { id: "e", text: "Increase the maximum response length" },
      { id: "f", text: "Add more stop sequences" },
      { id: "g", text: "Choose a model with a larger context window" },
      { id: "h", text: "Set top-k to 1" },
    ],
    correctOptionIds: ["a", "h"],
    answerType: "single",
    explanation: "Temperature scales the probability distribution the model samples from, so a low value concentrates probability on the most likely tokens and yields focused, repeatable wording; setting top-k to 1 goes further by always choosing the single most likely token.",
    optionRationale: {
      a: "A lower temperature makes the highest-probability tokens far more likely, giving consistent, conservative output.",
      b: "Raising temperature increases randomness, which is the opposite of what a summarizer needs here.",
      c: "A top-p near 1.0 admits almost the whole distribution, so more unusual tokens can be sampled.",
      d: "A larger top-k widens the candidate pool and therefore increases variety.",
      e: "Maximum response length caps how long the answer may be; it does not affect word choice.",
      f: "Stop sequences end generation at a marker; they do not make the wording more consistent.",
      g: "A larger context window lets you send more input and says nothing about output consistency.",
      h: "With top-k set to 1 the model always picks the single most likely token, which is greedy decoding and gives the most repeatable wording.",
    },
    referenceUrl: "https://docs.aws.amazon.com/bedrock/latest/userguide/inference-parameters.html",
    referenceLabel: "Influence response generation with inference parameters",
    consoleUrl: "https://console.aws.amazon.com/bedrock/",
    consoleLabel: "Amazon Bedrock console",
  },
  {
    id: "aif-gen5",
    domain: "genai-fundamentals",
    text: "A developer wants the model to sample only from the smallest set of next tokens whose probabilities add up to 0.9. Which inference parameter does this?",
    options: [
      { id: "a", text: "Top-p" },
      { id: "b", text: "Top-k" },
      { id: "c", text: "Temperature" },
      { id: "d", text: "Maximum response length" },
      { id: "e", text: "Stop sequences" },
      { id: "f", text: "Number of provisioned model units" },
      { id: "g", text: "Nucleus sampling" },
      { id: "h", text: "Frequency penalty" },
    ],
    correctOptionIds: ["a", "g"],
    answerType: "single",
    explanation: "Top-p, also called nucleus sampling, keeps the shortest list of candidate tokens whose cumulative probability reaches the given value, so the size of the candidate set changes with the model's confidence; top-k instead keeps a fixed number of candidates regardless of their probabilities.",
    optionRationale: {
      a: "Top-p is defined by a cumulative probability threshold, exactly as described.",
      b: "Top-k keeps a fixed count of the most likely tokens, not a probability total.",
      c: "Temperature reshapes the whole distribution rather than truncating it at a probability sum.",
      d: "Maximum response length limits output tokens and has no role in choosing candidates.",
      e: "Stop sequences tell the model where to stop generating text.",
      f: "Model units are a Provisioned Throughput capacity setting, not an inference parameter.",
      g: "Nucleus sampling is the other name for top-p, which keeps the smallest set of tokens whose cumulative probability reaches the threshold.",
      h: "A frequency penalty lowers the likelihood of tokens that have already appeared rather than truncating candidates at a probability total.",
    },
    referenceUrl: "https://docs.aws.amazon.com/bedrock/latest/userguide/inference-parameters.html",
    referenceLabel: "Influence response generation with inference parameters",
    consoleUrl: "https://console.aws.amazon.com/bedrock/",
    consoleLabel: "Amazon Bedrock console",
  },
  {
    id: "aif-gen6",
    domain: "genai-fundamentals",
    text: "Why did transformer-based architectures largely replace earlier recurrent networks for large language models?",
    options: [
      { id: "a", text: "Self-attention lets the model weigh how every token in the input relates to every other token and process them in parallel, so training scales to huge corpora and long-range context is preserved" },
      { id: "b", text: "Transformers process one token at a time in strict order, which trains faster than parallel designs" },
      { id: "c", text: "Transformers need no training data, because attention is a hand-written rule set" },
      { id: "d", text: "Transformers remove the need for accelerated hardware such as GPUs" },
      { id: "e", text: "Transformers store the training corpus verbatim and look up the matching answer" },
      { id: "f", text: "Transformers train only on labeled data, which is why they are more accurate" },
      { id: "g", text: "Transformers return the identical output for the same input every time" },
      { id: "h", text: "Without recurrence, all positions in a sequence can be trained in parallel, which made scaling to huge datasets practical" },
    ],
    correctOptionIds: ["a", "h"],
    answerType: "single",
    explanation: "The transformer's attention mechanism computes relationships between all positions in a sequence at once, which removes the sequential bottleneck of recurrent networks, allows massive parallel training, and keeps distant context available.",
    optionRationale: {
      a: "Parallel processing plus attention over the whole sequence is the transformer's defining advantage.",
      b: "Strictly sequential processing is the recurrent network behavior that transformers avoid.",
      c: "Transformers are trained statistically on very large text corpora; attention weights are learned.",
      d: "Training and serving large transformers is hardware-hungry and usually needs GPUs or custom accelerators.",
      e: "Models learn statistical patterns rather than storing a retrievable copy of the corpus.",
      f: "Pre-training uses vast amounts of unlabeled text; labels are only used for later customization.",
      g: "Sampling makes generative output vary between calls unless decoding is heavily constrained.",
      h: "Dropping step-by-step recurrence allows parallel computation across the sequence on accelerators, which is what made training on web-scale corpora feasible.",
    },
    referenceUrl: "https://aws.amazon.com/what-is/transformers-in-artificial-intelligence/",
    referenceLabel: "What are transformers in artificial intelligence? (AWS)",
  },
  {
    id: "aif-gen7",
    domain: "genai-fundamentals",
    text: "A marketing team wants a model that creates original product imagery from a written description. Which model family is designed for this, and how does it work at a high level?",
    options: [
      { id: "a", text: "A diffusion model, which starts from random noise and removes the noise step by step, guided by the text prompt, until an image emerges" },
      { id: "b", text: "A large language model, which predicts the next word in a sequence of text" },
      { id: "c", text: "A convolutional image classifier, which assigns each image one category label" },
      { id: "d", text: "A linear regression model, which fits a line through labeled pixels" },
      { id: "e", text: "A k-means clustering model, which groups similar images together" },
      { id: "f", text: "A recurrent network, which converts recorded speech into text" },
      { id: "g", text: "A decision tree, which splits pixels on threshold rules" },
      { id: "h", text: "A generative adversarial network, where a generator creates images and a discriminator judges their realism" },
    ],
    correctOptionIds: ["a", "h"],
    answerType: "single",
    explanation: "Text-to-image generation is done mainly by diffusion models, which learn to reverse a noising process and are steered by the prompt while denoising, and also by generative adversarial networks; Amazon Nova Canvas and Stability AI's Stable Diffusion models are available through the Amazon Bedrock API.",
    optionRationale: {
      a: "Iterative denoising from random noise, conditioned on a prompt, is how diffusion image models work.",
      b: "A language model generates text; it does not produce pixels.",
      c: "A classifier labels an existing image and creates nothing new.",
      d: "Linear regression predicts a number from features and cannot synthesize an image.",
      e: "Clustering groups existing images without generating any.",
      f: "Speech-to-text converts audio into words, which is a different task entirely.",
      g: "A decision tree makes rule-based predictions and has no generative capability.",
      h: "Generative adversarial networks pit a generator against a discriminator and can create realistic images from text prompts.",
    },
    referenceUrl: "https://aws.amazon.com/what-is/stable-diffusion/",
    referenceLabel: "What is Stable Diffusion? (AWS)",
    consoleUrl: "https://console.aws.amazon.com/bedrock/",
    consoleLabel: "Amazon Bedrock console",
  },
  {
    id: "aif-gen8",
    domain: "genai-fundamentals",
    text: "A logistics company wants one model that can accept a photograph of a damaged pallet together with a written question about it and answer in text. Which kind of model is needed?",
    options: [
      { id: "a", text: "A multimodal foundation model, which accepts more than one input type, such as text and images, in the same request" },
      { id: "b", text: "A text-only large language model" },
      { id: "c", text: "An embedding model, which returns a vector for each input" },
      { id: "d", text: "A text-to-speech model" },
      { id: "e", text: "A text-to-image diffusion model" },
      { id: "f", text: "A tabular gradient boosting model" },
      { id: "g", text: "A speech-to-text transcription model" },
      { id: "h", text: "A vision-language model" },
    ],
    correctOptionIds: ["a", "h"],
    answerType: "single",
    explanation: "Multimodal models, such as vision-language models, take combinations of input types, such as an image plus a text question, and respond in text; Amazon Nova understanding models and Anthropic Claude models on Amazon Bedrock support image and text input through the same API.",
    optionRationale: {
      a: "Accepting an image and a text question together in one request is the definition of a multimodal model.",
      b: "A text-only model cannot take the photograph as input.",
      c: "An embedding model returns vectors for search or comparison rather than an answer in words.",
      d: "Text-to-speech produces audio from text and does not analyze an image.",
      e: "A diffusion model generates an image from text, which is the reverse of what is required.",
      f: "Gradient boosting works on structured columns, not on photographs and free-text questions.",
      g: "Transcription turns audio into text and does not process images.",
      h: "A vision-language model takes an image and text together and generates a text answer, which is exactly the requirement.",
    },
    referenceUrl: "https://docs.aws.amazon.com/nova/latest/userguide/what-is-nova.html",
    referenceLabel: "What is Amazon Nova?",
    consoleUrl: "https://console.aws.amazon.com/bedrock/",
    consoleLabel: "Amazon Bedrock console",
  },
  {
    id: "aif-gen9",
    domain: "genai-fundamentals",
    text: "A data science manager is explaining to stakeholders how a foundation model differs from the task-specific models the team has built before. Which statement is accurate?",
    options: [
      { id: "a", text: "A foundation model is pretrained on a very large, mostly unlabeled corpus and can then be adapted to many different downstream tasks, whereas a task-specific model is trained on labeled data for one task" },
      { id: "b", text: "A foundation model is always smaller and cheaper to run than a task-specific model" },
      { id: "c", text: "A foundation model needs no evaluation, because it was already validated by its provider" },
      { id: "d", text: "A foundation model has to be retrained from scratch for every new prompt" },
      { id: "e", text: "Task-specific models cannot be deployed behind an API endpoint" },
      { id: "f", text: "Foundation models work only on images, while task-specific models work only on text" },
      { id: "g", text: "A foundation model requires a labeled example for every output it could ever produce" },
      { id: "h", text: "A foundation model adapts to new tasks through prompting or fine-tuning, while a task-specific model is limited to its trained task" },
    ],
    correctOptionIds: ["a", "h"],
    answerType: "single",
    explanation: "Foundation models are pretrained once at great expense on broad unlabeled data and are general enough to serve many tasks through prompting or customization, while a task-specific model is trained on labeled examples of one narrow task.",
    optionRationale: {
      a: "Broad unlabeled pre-training plus adaptability to many tasks is what makes a model a foundation model.",
      b: "Foundation models are usually far larger and more expensive per prediction than a narrow model.",
      c: "You still have to evaluate a foundation model on your own data and task before trusting it.",
      d: "Prompting uses the already-trained model; no retraining happens per request.",
      e: "Task-specific models are routinely hosted behind real-time endpoints.",
      f: "Both kinds of model span text, images, audio, and tabular data.",
      g: "Needing a label for every possible output describes supervised learning, not foundation models.",
      h: "Adaptability to many downstream tasks without training a new model from scratch is the defining trait of a foundation model.",
    },
    referenceUrl: "https://aws.amazon.com/what-is/foundation-models/",
    referenceLabel: "What are foundation models? (AWS)",
  },
  {
    id: "aif-gen10",
    domain: "genai-fundamentals",
    text: "A bank is deciding which backlog items to build on a text foundation model. Which TWO tasks are the strongest fit for a generative text model?",
    options: [
      { id: "a", text: "Producing a short plain-language summary of a long customer complaint thread" },
      { id: "b", text: "Drafting a reply to a customer question in natural language for an agent to review and send" },
      { id: "c", text: "Calculating the exact interest owed on each loan account at month end" },
      { id: "d", text: "Storing 20 TB of transaction records for a seven-year regulatory retention period" },
      { id: "e", text: "Applying the fixed rule that any transfer above 10,000 dollars needs a second approver" },
      { id: "f", text: "Distributing web traffic evenly across two Availability Zones" },
      { id: "g", text: "Producing a general ledger total that must reconcile to the cent every night" },
      { id: "h", text: "Translating customer messages into the language the support agent reads" },
      { id: "i", text: "Answering staff questions about internal policy documents through a chat assistant" },
      { id: "j", text: "Assigning unique sequential account numbers to newly opened accounts" },
    ],
    correctOptionIds: ["a", "b", "h", "i"],
    answerType: "multi",
    explanation: "Generative text models are strong at summarization, drafting, translation, classification of free text, and conversational assistance; exact arithmetic, durable storage, fixed rules, and traffic distribution belong to conventional software that is deterministic and auditable.",
    optionRationale: {
      a: "Summarizing long free text into a short readable form is a core generative language task.",
      b: "Drafting a natural language reply for human review is a standard assisted-response use case.",
      c: "Interest is a deterministic calculation that must be exact, so ordinary code is the right tool.",
      d: "Long-term retention is a storage requirement met by services such as Amazon S3.",
      e: "A fixed, regulated threshold should be enforced by explicit business logic, not a sampled model.",
      f: "Traffic distribution is handled by a load balancer, not by any model.",
      g: "A ledger total must reconcile exactly, which rules out a probabilistic generator.",
      h: "Translation between natural languages is a strong generative text task that needs fluent output rather than exact computation.",
      i: "Conversational question answering over policy text plays to a generative model's language strengths, especially when grounded in the source documents.",
      j: "Account numbers must never collide or skip, which calls for a database sequence rather than a probabilistic generator.",
    },
    referenceUrl: "https://aws.amazon.com/generative-ai/",
    referenceLabel: "Generative AI on AWS",
  },
  {
    id: "aif-gen11",
    domain: "genai-fundamentals",
    text: "Developers want inline code suggestions inside their IDE, the ability to ask questions about their existing repository in natural language, and help drafting routine framework upgrades. Which AWS service is purpose-built for this?",
    options: [
      { id: "a", text: "The Kiro IDE" },
      { id: "b", text: "Amazon Q Business" },
      { id: "c", text: "PartyRock" },
      { id: "d", text: "Amazon SageMaker JumpStart" },
      { id: "e", text: "Amazon Comprehend" },
      { id: "f", text: "Amazon Kendra" },
      { id: "g", text: "AWS CodePipeline" },
      { id: "h", text: "Kiro" },
    ],
    correctOptionIds: ["a", "h"],
    answerType: "single",
    explanation: "Kiro is the AWS agentic development environment, available as an IDE and a CLI, with inline suggestions, chat about your own code, and agents that plan and apply multi-file changes such as framework upgrades. It succeeds Amazon Q Developer, which stopped accepting new sign-ups on May 15, 2026 and whose IDE plugins reach end of support on April 30, 2027.",
    optionRationale: {
      a: "The Kiro IDE offers inline suggestions and agentic chat grounded in the open project, and it can plan and apply multi-file changes such as a framework upgrade.",
      b: "Q Business answers employee questions over enterprise content; it is not a coding assistant.",
      c: "PartyRock is a no-code playground for building small generative AI apps, not an IDE companion.",
      d: "JumpStart is a model hub for deploying and tuning models, not a coding assistant.",
      e: "Comprehend extracts entities, sentiment, and key phrases from text.",
      f: "Kendra provides intelligent enterprise search over documents.",
      g: "CodePipeline automates build and release stages; it writes no code for you.",
      h: "Kiro is the AWS agentic IDE and CLI, with inline coding assistance, chat about your own codebase, and agent-driven help with code upgrades.",
    },
    referenceUrl: "https://docs.aws.amazon.com/amazonq/latest/qdeveloper-ug/q-developer-ide-end-of-support.html",
    referenceLabel: "Amazon Q Developer IDE plugins end of support (move to Kiro)",
  },
  {
    id: "aif-gen12",
    domain: "genai-fundamentals",
    text: "A retailer wants to generate on-brand lifestyle imagery from text prompts, with no ML infrastructure to manage and no model training. Which approach fits best?",
    options: [
      { id: "a", text: "Call a text-to-image model, such as a Stability AI model, through the Amazon Bedrock API" },
      { id: "b", text: "Train a convolutional neural network from scratch in Amazon SageMaker AI" },
      { id: "c", text: "Use Amazon Rekognition to generate the images" },
      { id: "d", text: "Use Amazon Textract to generate the images" },
      { id: "e", text: "Use Amazon Polly to render the images" },
      { id: "f", text: "Use Amazon Q Developer to generate the images in the IDE" },
      { id: "g", text: "Use Amazon Comprehend to create the images" },
      { id: "h", text: "Use Stability AI Style Guide on Amazon Bedrock with a reference image of the brand's look" },
    ],
    correctOptionIds: ["a", "h"],
    answerType: "single",
    explanation: "Amazon Bedrock offers image generation models, such as Stability AI's Stable Diffusion 3.5 Large and Stable Image models, through a serverless API, so a team can generate images from prompts, optionally guided by a reference image in the brand's style, without provisioning infrastructure or training anything. Amazon Nova Canvas, which also generated images on Bedrock, is a Legacy model that new customers cannot adopt and that reaches end of life on September 30, 2026.",
    optionRationale: {
      a: "Bedrock provides managed access to text-to-image models, which is exactly the requirement.",
      b: "Training a network from scratch is slow and expensive and is not needed when pretrained models exist.",
      c: "Rekognition analyzes existing images and video; it does not create new images.",
      d: "Textract extracts text and data from scanned documents.",
      e: "Polly converts text into lifelike speech, producing audio rather than images.",
      f: "Q Developer assists with code; it is not an image generation service.",
      g: "Comprehend performs natural language processing on text.",
      h: "Style Guide on Amazon Bedrock takes a text prompt and a reference image, extracts the image's style, and generates new images in that style serverlessly with no training.",
    },
    referenceUrl: "https://docs.aws.amazon.com/bedrock/latest/userguide/stable-image-services.html",
    referenceLabel: "Stability AI Image Services on Amazon Bedrock (Style Guide)",
    consoleUrl: "https://console.aws.amazon.com/bedrock/",
    consoleLabel: "Amazon Bedrock console",
  },
  {
    id: "aif-gen13",
    domain: "genai-fundamentals",
    text: "A legal team tests an assistant built on a foundation model and finds that it cites a court case that does not exist, stated with complete confidence. What is this behavior, and what does it imply for the design?",
    options: [
      { id: "a", text: "A hallucination: the model can generate fluent content that is simply untrue, so output used in a regulated process must be grounded in trusted sources and reviewed by a person" },
      { id: "b", text: "Overfitting: the model memorized its training set, so it needs more training epochs" },
      { id: "c", text: "Data drift: the production data has moved away from the training data, so the endpoint must be scaled up" },
      { id: "d", text: "A throttling error: the account needs a higher requests-per-minute quota" },
      { id: "e", text: "Underfitting: the model is too small, so the context window must be enlarged" },
      { id: "f", text: "Label leakage: the target value was present among the input features" },
      { id: "g", text: "A cold start: the endpoint needs to warm up before its answers become accurate" },
      { id: "h", text: "A confabulation: the model made up a plausible fact, so regulated use needs grounding in trusted sources and human review" },
    ],
    correctOptionIds: ["a", "h"],
    answerType: "single",
    explanation: "A hallucination, also called a confabulation, is confident, plausible-sounding output that is factually wrong; because the model optimizes for likely text rather than truth, high-risk uses need authoritative source material supplied with the request and human review of the result.",
    optionRationale: {
      a: "Inventing a convincing but non-existent citation is the textbook example of a hallucination.",
      b: "Overfitting describes a model that performs well on training data and poorly on new data, and more epochs would worsen it.",
      c: "Drift concerns changing input distributions over time, not a fabricated fact in one answer.",
      d: "Throttling returns an error rather than a wrong answer.",
      e: "Underfitting means the model fails to capture patterns; the context window is unrelated to truthfulness.",
      f: "Label leakage is a supervised training data problem, not a generation behavior.",
      g: "Cold start affects latency on the first request, not factual accuracy.",
      h: "Confabulation is the term NIST and others use for confidently generated false content, the same failure as a hallucination.",
    },
    referenceUrl: "https://docs.aws.amazon.com/wellarchitected/latest/generative-ai-lens/generative-ai-lens.html",
    referenceLabel: "AWS Well-Architected Generative AI Lens",
  },
  {
    id: "aif-gen14",
    domain: "genai-fundamentals",
    text: "A QA engineer raises a defect: sending the identical prompt to the identical foundation model twice returns two differently worded answers. What is the most accurate response?",
    options: [
      { id: "a", text: "This is expected behavior, because the model samples each next token from a probability distribution; lowering temperature and top-p makes responses far more consistent, though identical text is still not guaranteed" },
      { id: "b", text: "The model is faulty and must be redeployed" },
      { id: "c", text: "The model's knowledge cutoff has passed, so its answers change over time" },
      { id: "d", text: "The account is being throttled, which silently truncates responses" },
      { id: "e", text: "Two different model versions must be serving the traffic; nothing else can cause this" },
      { id: "f", text: "The context window was exceeded, so the prompt was silently cut in half" },
      { id: "g", text: "The model must be fine-tuned before it can return consistent answers" },
      { id: "h", text: "This is expected from a probabilistic model, so tests should check meaning and format rather than exact wording" },
    ],
    correctOptionIds: ["a", "h"],
    answerType: "single",
    explanation: "Generative models are nondeterministic by design: the next token is sampled from a distribution, so wording varies between calls. Test suites therefore assert on meaning, format, or required content rather than exact string equality.",
    optionRationale: {
      a: "Sampling explains the variation, and the decoding parameters control how much of it there is.",
      b: "Varying wording is normal model behavior and is not evidence of a broken deployment.",
      c: "The knowledge cutoff is fixed for a model version and does not change answers call by call.",
      d: "Throttling produces an explicit error rather than quietly reworded output.",
      e: "Even a single model version produces varied wording, so this is not the only possible cause.",
      f: "Exceeding the context window raises a validation error instead of silently truncating.",
      g: "Fine-tuning changes style and task behavior but does not remove sampling randomness.",
      h: "Sampled output naturally varies in wording, so the right fix is to change the test's assertions rather than treat the variation as a defect.",
    },
    referenceUrl: "https://docs.aws.amazon.com/bedrock/latest/userguide/inference-parameters.html",
    referenceLabel: "Influence response generation with inference parameters",
    consoleUrl: "https://console.aws.amazon.com/bedrock/",
    consoleLabel: "Amazon Bedrock console",
  },
  {
    id: "aif-gen15",
    domain: "genai-fundamentals",
    text: "A support assistant built on a foundation model gives wrong details about a product the company launched last month, although it answers questions about older products correctly. What is the most likely explanation?",
    options: [
      { id: "a", text: "The model's training data has a cutoff date, so it never saw the new product; the application has to supply those facts with the request or use a model customized with them" },
      { id: "b", text: "The temperature is set too low, which removes recent facts" },
      { id: "c", text: "The context window is too large, which dilutes the answer" },
      { id: "d", text: "The model is overfitted to the new product" },
      { id: "e", text: "The model needs Provisioned Throughput before it can answer accurately" },
      { id: "f", text: "Tokenization has failed for the product name" },
      { id: "g", text: "The embedding dimension is too small for recent data" },
      { id: "h", text: "The launch postdates the training data, so the model has to be given those facts" },
    ],
    correctOptionIds: ["a", "h"],
    answerType: "single",
    explanation: "A foundation model only knows what was in its training corpus up to its knowledge cutoff, so anything newer must be provided at request time or added through customization; the model will otherwise answer from stale or invented knowledge.",
    optionRationale: {
      a: "A knowledge cutoff is exactly why a model is confidently wrong about very recent events.",
      b: "Temperature controls randomness of wording and cannot add facts the model never saw.",
      c: "A larger context window is a capacity to send more input, never a cause of wrong facts.",
      d: "Overfitting requires the model to have trained on the product, which is the opposite of the situation.",
      e: "Provisioned Throughput buys capacity; it has no effect on what the model knows.",
      f: "Tokenization handles unfamiliar words routinely by splitting them into sub-word pieces.",
      g: "Embedding dimension is a property of embedding models and unrelated to training recency.",
      h: "The model learned nothing after its training data was collected, so newer product facts have to be supplied with the request or through customization.",
    },
    referenceUrl: "https://aws.amazon.com/what-is/generative-ai/",
    referenceLabel: "What is generative AI? (AWS)",
  },
  {
    id: "aif-gen16",
    domain: "genai-fundamentals",
    text: "An architect is writing the risk section of a design that uses a text foundation model. Which TWO of these are genuine limitations of foundation models that the design must allow for?",
    options: [
      { id: "a", text: "Output can be factually wrong while reading as confident and authoritative" },
      { id: "b", text: "It is difficult to explain exactly why the model produced one particular output" },
      { id: "c", text: "The model cannot accept an input longer than a single sentence" },
      { id: "d", text: "The model returns the identical response for a prompt every time, so experiments cannot be varied" },
      { id: "e", text: "Foundation models cannot be invoked through an API" },
      { id: "f", text: "The model must be retrained before each request it serves" },
      { id: "g", text: "Generated text cannot be logged or reviewed afterwards" },
      { id: "h", text: "Its knowledge stops at a training cutoff date, so recent events are unknown to it" },
      { id: "i", text: "Output can reflect social biases present in the training data" },
      { id: "j", text: "The model cannot follow instructions given in a system prompt" },
    ],
    correctOptionIds: ["a", "b", "h", "i"],
    answerType: "multi",
    explanation: "Real designs must allow for factual unreliability, low interpretability, a knowledge cutoff after which the model knows nothing new, and biases absorbed from training data; these push teams toward grounding, human review, evaluation, and logging.",
    optionRationale: {
      a: "Confident but incorrect output, commonly called hallucination, is a well-known limitation.",
      b: "Interpretability is genuinely hard for large models, which complicates audit and debugging.",
      c: "Models accept thousands to hundreds of thousands of tokens, far more than one sentence.",
      d: "Generative models are nondeterministic; identical repeated output is not the normal behavior.",
      e: "API invocation is the usual way to use them, for example through Amazon Bedrock.",
      f: "Inference uses the already-trained model; nothing is retrained per request.",
      g: "Requests and responses can and should be logged for review and troubleshooting.",
      h: "A model only knows what was in its training data, so anything after the cutoff has to be supplied by the application.",
      i: "Models absorb biases from the large corpora they are trained on, so outputs need evaluation and guardrails for fairness.",
      j: "System prompts and instructions are a standard, effective way to steer foundation model behavior.",
    },
    referenceUrl: "https://aws.amazon.com/what-is/foundation-models/",
    referenceLabel: "What are foundation models? (AWS)",
  },
  {
    id: "aif-gen17",
    domain: "genai-fundamentals",
    text: "A team is documenting the foundation model lifecycle for its governance board. Which sequence describes that lifecycle correctly?",
    options: [
      { id: "a", text: "Select data, pre-train the model, customize it for the use case, evaluate it, deploy it, then collect feedback that feeds the next round of customization" },
      { id: "b", text: "Customize the model, select data, deploy it, pre-train it, then evaluate it" },
      { id: "c", text: "Deploy the model, evaluate it, pre-train it, then select data" },
      { id: "d", text: "Pre-train the model, select data, deploy it, then customize it" },
      { id: "e", text: "Select data, deploy the model, pre-train it, customize it, then evaluate it" },
      { id: "f", text: "Evaluate the model, pre-train it, select data, collect feedback, then deploy it" },
      { id: "g", text: "Select a pre-trained model, customize it, evaluate it, deploy it, then gather feedback for the next iteration" },
      { id: "h", text: "Select data, customize the model, pre-train it, deploy it, then evaluate it" },
    ],
    correctOptionIds: ["a", "g"],
    answerType: "single",
    explanation: "Data selection comes first, pre-training produces the general model, customization adapts it to the use case, evaluation decides whether it is good enough, deployment puts it in front of users, and feedback from real use drives the next iteration; teams that start from an existing pre-trained model simply begin at model selection.",
    optionRationale: {
      a: "This is the accepted order, and the loop back from feedback is what makes it a lifecycle.",
      b: "Customization cannot precede the pre-training that creates the model being customized.",
      c: "Deploying before any data is chosen or the model is trained is not a workable order.",
      d: "Pre-training cannot happen before the training data has been selected.",
      e: "Deployment belongs after evaluation, not before the model has even been trained.",
      f: "Evaluation needs a trained model to measure, so it cannot come first.",
      g: "Teams that start from an existing foundation model begin at model selection, and the remaining stages follow in the same order with feedback closing the loop.",
      h: "Customization adapts an already pre-trained model, so it cannot precede pre-training, and evaluation belongs before deployment.",
    },
    referenceUrl: "https://docs.aws.amazon.com/wellarchitected/latest/generative-ai-lens/generative-ai-lens.html",
    referenceLabel: "AWS Well-Architected Generative AI Lens",
    diagram: `flowchart LR
  A[Select data] --> B[Pre-train foundation model]
  B --> C[Customize for the use case]
  C --> D[Evaluate]
  D --> E[Deploy]
  E --> F[Collect feedback]
  F --> C`,
  },
  {
    id: "aif-gen18",
    domain: "genai-fundamentals",
    text: "A team wants to try foundation models from several different providers behind one API, with no servers to provision or patch, and wants to be able to switch models later with minimal code change. Which AWS service is designed for this?",
    options: [
      { id: "a", text: "Amazon Bedrock" },
      { id: "b", text: "Amazon EC2 GPU instances running a self-managed inference server" },
      { id: "c", text: "Amazon EKS with a self-hosted model serving stack" },
      { id: "d", text: "Amazon Comprehend" },
      { id: "e", text: "Amazon Kendra" },
      { id: "f", text: "Amazon Q Business" },
      { id: "g", text: "AWS Glue" },
      { id: "h", text: "The Amazon Bedrock Converse API" },
    ],
    correctOptionIds: ["a", "h"],
    answerType: "single",
    explanation: "Amazon Bedrock is a fully managed, serverless service that exposes foundation models from several providers, including Amazon, Anthropic, Cohere, Meta, Mistral AI, AI21 Labs, and Stability AI, through a single API, and its Converse API uses one request format for all of them, so teams can compare models and change their choice without rebuilding the application.",
    optionRationale: {
      a: "A single managed API across many model providers is exactly what Bedrock offers.",
      b: "Self-managed GPU instances mean provisioning, patching, and scaling the serving stack yourself.",
      c: "Running models on EKS gives control but leaves all the operational work with the team.",
      d: "Comprehend performs fixed natural language processing tasks and offers no model choice.",
      e: "Kendra is an intelligent search service over enterprise documents.",
      f: "Q Business is a ready-made assistant application, not a multi-provider model API.",
      g: "Glue is a data integration and ETL service.",
      h: "The Converse API gives every supported Bedrock model the same serverless request format, so switching providers is mostly a change of model ID.",
    },
    referenceUrl: "https://docs.aws.amazon.com/bedrock/latest/userguide/what-is-bedrock.html",
    referenceLabel: "What is Amazon Bedrock?",
    consoleUrl: "https://console.aws.amazon.com/bedrock/",
    consoleLabel: "Amazon Bedrock console",
  },
  {
    id: "aif-gen19",
    domain: "genai-fundamentals",
    text: "A media company will run a steady, high volume of Amazon Bedrock captioning requests all day for the next six months and needs a guaranteed level of model throughput with a predictable monthly cost. Which purchasing option fits best?",
    options: [
      { id: "a", text: "Buy Provisioned Throughput for the model, which reserves model units for a committed term at an hourly rate" },
      { id: "b", text: "Stay on on-demand pricing, which charges per input and output token with no commitment" },
      { id: "c", text: "Buy Amazon EC2 Reserved Instances for the Bedrock capacity" },
      { id: "d", text: "Request a higher requests-per-minute service quota, which reserves capacity for the account" },
      { id: "e", text: "Switch the traffic to batch inference, which guarantees interactive throughput" },
      { id: "f", text: "Lower the temperature so that each response consumes fewer tokens" },
      { id: "g", text: "Buy an EC2 Savings Plan, which also covers Bedrock token usage" },
      { id: "h", text: "Use the Amazon Bedrock Reserved service tier, which guarantees tokens-per-minute capacity for a fixed monthly price" },
    ],
    correctOptionIds: ["a", "h"],
    answerType: "single",
    explanation: "On-demand invocation charges per token with no commitment and shares capacity with other customers, while Provisioned Throughput reserves model units for a committed term and the Reserved service tier reserves tokens-per-minute capacity at a fixed monthly price, both of which suit sustained high-volume traffic that needs predictable capacity and cost.",
    optionRationale: {
      a: "Provisioned Throughput is the Bedrock option that reserves capacity at a fixed hourly price for a term.",
      b: "On-demand is flexible but gives no throughput guarantee and a bill that varies with usage.",
      c: "Bedrock is serverless; there are no EC2 instances for you to reserve.",
      d: "A quota increase raises a limit but does not reserve dedicated model capacity.",
      e: "Batch inference trades latency for a lower price on large offline jobs; it is not for interactive traffic.",
      f: "Temperature changes wording, not the throughput available to the workload.",
      g: "EC2 Savings Plans cover compute usage, not Bedrock model invocation.",
      h: "The Reserved tier reserves guaranteed tokens-per-minute capacity for a one- or three-month term at a fixed price billed monthly.",
    },
    referenceUrl: "https://docs.aws.amazon.com/bedrock/latest/userguide/prov-throughput.html",
    referenceLabel: "Increase model invocation capacity with Provisioned Throughput",
    consoleUrl: "https://console.aws.amazon.com/bedrock/",
    consoleLabel: "Amazon Bedrock console",
  },
  {
    id: "aif-gen20",
    domain: "genai-fundamentals",
    text: "Which TWO advantages does consuming foundation models through Amazon Bedrock offer compared with hosting an open weights model yourself on GPU instances?",
    options: [
      { id: "a", text: "There is no inference infrastructure to provision, patch, or scale, because access is through a managed API" },
      { id: "b", text: "A single API lets the team compare models from several providers and switch between them with little rework" },
      { id: "c", text: "Bedrock removes the need to evaluate the quality of model output" },
      { id: "d", text: "Bedrock guarantees that every generated answer is factually correct" },
      { id: "e", text: "Bedrock is free of charge for production traffic" },
      { id: "f", text: "Every AWS Region offers exactly the same set of Bedrock models" },
      { id: "g", text: "Every prompt sent to Bedrock is used to train the provider's base model, so the model keeps improving" },
      { id: "h", text: "On-demand pricing is per token, so the team pays nothing for idle GPU capacity between requests" },
      { id: "i", text: "Managed features such as Guardrails and Knowledge Bases can be added without building them" },
      { id: "j", text: "Bedrock gives the team administrator access to the GPU servers that run each model" },
    ],
    correctOptionIds: ["a", "b", "h", "i"],
    answerType: "multi",
    explanation: "Bedrock's value is operational and strategic: AWS runs the serving infrastructure with per-token pricing and built-in features such as Guardrails and Knowledge Bases, and one API spanning many providers keeps the choice of model reversible. It does not remove the need to evaluate output, it is not free, model availability differs by Region, and your prompts and completions are not used to train the base models.",
    optionRationale: {
      a: "Serverless access removes the provisioning, patching, and scaling work of self-hosting.",
      b: "One API across providers is what makes swapping models cheap later on.",
      c: "Evaluating output for accuracy and fitness remains the customer's responsibility.",
      d: "No service can guarantee factual correctness of generated text.",
      e: "Bedrock charges per token on demand or per model unit for Provisioned Throughput.",
      f: "Model availability varies by Region, which is a real planning constraint.",
      g: "Amazon Bedrock does not use your prompts or completions to train the base foundation models.",
      h: "Pay-per-token billing charges only for actual use, whereas self-hosted GPU instances bill for every hour they run, busy or idle.",
      i: "Bedrock ships safety filtering, managed retrieval, and evaluation as built-in features that a self-hosting team would have to build and run itself.",
      j: "Bedrock is serverless, so AWS runs the hosts and customers never get access to the underlying servers.",
    },
    referenceUrl: "https://aws.amazon.com/bedrock/",
    referenceLabel: "Amazon Bedrock",
    consoleUrl: "https://console.aws.amazon.com/bedrock/",
    consoleLabel: "Amazon Bedrock console",
  },
  {
    id: "aif-gen21",
    domain: "genai-fundamentals",
    text: "A company wants an internal assistant that employees can ask questions in plain language, answering from the company's own documents in Amazon S3 and SharePoint, respecting each employee's existing access permissions, and it wants to build this with as little custom code as possible. Which service fits best?",
    options: [
      { id: "a", text: "A chat agent in Amazon Quick" },
      { id: "b", text: "Amazon Q Developer" },
      { id: "c", text: "PartyRock" },
      { id: "d", text: "A custom application built directly on the Amazon Bedrock API" },
      { id: "e", text: "Amazon Lex" },
      { id: "f", text: "Amazon Polly" },
      { id: "g", text: "Amazon Transcribe" },
      { id: "h", text: "Amazon Quick" },
    ],
    correctOptionIds: ["a", "h"],
    answerType: "single",
    explanation: "Amazon Quick, the successor to Amazon Q Business (closed to new customers on July 30, 2026), is a managed generative AI assistant for employees: it ships with connectors to common enterprise content sources such as Amazon S3 and SharePoint, can enforce the document-level permissions users already have in those sources, and its chat agents are configured rather than coded.",
    optionRationale: {
      a: "Quick chat agents answer from knowledge bases built on connectors such as Amazon S3 and SharePoint, and with document-level ACLs enabled each employee sees only content they can already access.",
      b: "Q Developer is aimed at writing and maintaining software, not at answering general employee questions.",
      c: "PartyRock is a playground for experimenting with app ideas, not an enterprise assistant over internal data.",
      d: "Building on Bedrock is possible but means writing the ingestion, retrieval, and permission handling yourself, which contradicts the low-code requirement.",
      e: "Lex builds scripted conversational bots around intents and slots.",
      f: "Polly converts text to speech.",
      g: "Transcribe converts speech to text.",
      h: "Amazon Quick, the successor to Q Business, connects to sources such as Amazon S3 and SharePoint, enforces document-level permissions, and answers employees' questions with no custom code.",
    },
    referenceUrl: "https://docs.aws.amazon.com/quick/latest/userguide/what-is.html",
    referenceLabel: "What is Amazon Quick?",
  },
  {
    id: "aif-gen22",
    domain: "genai-fundamentals",
    text: "A product manager who does not write code and has no AWS account wants to try out a small generative AI app idea in a browser before asking engineering to fund it. Which option is designed for that?",
    options: [
      { id: "a", text: "PartyRock, a hands-on Amazon Bedrock playground for building and sharing generative AI apps without writing code" },
      { id: "b", text: "An Amazon SageMaker Studio notebook" },
      { id: "c", text: "A Lambda function that calls the Amazon Bedrock InvokeModel API" },
      { id: "d", text: "An Amazon EC2 instance launched from a Deep Learning AMI" },
      { id: "e", text: "Amazon Q Developer inside an IDE" },
      { id: "f", text: "AWS CloudShell with the AWS CLI" },
      { id: "g", text: "An Amazon SageMaker JumpStart endpoint deployment" },
      { id: "h", text: "A prompt-driven Bedrock app builder that uses a social sign-in" },
    ],
    correctOptionIds: ["a", "h"],
    answerType: "single",
    explanation: "PartyRock is a browser playground built on Amazon Bedrock where anyone can sign in with a social login and then assemble and share a small generative AI app from prompts alone, which makes it the usual starting point for non-developers exploring an idea.",
    optionRationale: {
      a: "No code, no AWS account, and a browser interface match this requirement precisely.",
      b: "A Studio notebook requires an AWS account and Python.",
      c: "Writing a Lambda function is development work and needs an account.",
      d: "Launching and managing an EC2 instance is well beyond a no-code exploration.",
      e: "Q Developer lives in a developer's IDE and assumes the user writes code.",
      f: "CloudShell is a command line environment inside the AWS Management Console.",
      g: "Deploying a JumpStart endpoint means managing SageMaker AI infrastructure and cost.",
      h: "This describes PartyRock, which builds a shareable generative AI app from plain-language prompts and needs only a Google, Apple, or Amazon login rather than an AWS account.",
    },
    referenceUrl: "https://partyrock.aws/",
    referenceLabel: "PartyRock, an Amazon Bedrock playground",
  },
  {
    id: "aif-gen23",
    domain: "genai-fundamentals",
    text: "A regulated customer must run a specific open weights foundation model on infrastructure inside its own account and VPC, choosing the instance type and pinning the exact model version. Which AWS capability is the best starting point?",
    options: [
      { id: "a", text: "Amazon SageMaker JumpStart, which deploys a pretrained foundation model to a SageMaker AI endpoint in the customer's own account" },
      { id: "b", text: "On-demand invocation of a serverless model in Amazon Bedrock" },
      { id: "c", text: "PartyRock" },
      { id: "d", text: "Amazon Q Business" },
      { id: "e", text: "Amazon Comprehend custom classification" },
      { id: "f", text: "Amazon Rekognition Custom Labels" },
      { id: "g", text: "Amazon Polly with a custom lexicon" },
      { id: "h", text: "Amazon Bedrock Marketplace" },
    ],
    correctOptionIds: ["a", "h"],
    answerType: "single",
    explanation: "SageMaker JumpStart and Amazon Bedrock Marketplace both deploy a chosen pretrained model to a SageMaker AI endpoint the customer owns, so the team picks the instance type, keeps the model version fixed, and runs inference inside its own VPC.",
    optionRationale: {
      a: "JumpStart gives account-level control of the hosting, the instance type, and the model version.",
      b: "On-demand invocation of a serverless Bedrock model leaves AWS to choose and run the infrastructure, so the team cannot select instances or place the model in its own VPC.",
      c: "PartyRock is a public playground with no infrastructure control at all.",
      d: "Q Business is a managed assistant application, not a way to host a chosen model.",
      e: "Comprehend custom classification trains a text classifier and is not a foundation model host.",
      f: "Rekognition Custom Labels trains an image classifier for your own labels.",
      g: "A Polly lexicon adjusts pronunciation in speech synthesis.",
      h: "Bedrock Marketplace deploys a chosen model to a SageMaker AI endpoint in the customer's own account, with the instance type, instance count, and VPC settings the team selects.",
    },
    referenceUrl: "https://docs.aws.amazon.com/sagemaker/latest/dg/jumpstart-foundation-models.html",
    referenceLabel: "Amazon SageMaker JumpStart foundation models",
    consoleUrl: "https://console.aws.amazon.com/sagemaker/",
    consoleLabel: "Amazon SageMaker AI console",
  },
  {
    id: "aif-gen24",
    domain: "genai-fundamentals",
    text: "An executive sponsor asks how the success of a new generative AI customer assistant will be reported to the business. Which TWO of these are business value metrics rather than model or infrastructure metrics?",
    options: [
      { id: "a", text: "Customer satisfaction score for conversations the assistant handled" },
      { id: "b", text: "Average revenue per user among customers who used the assistant" },
      { id: "c", text: "Average number of tokens consumed per request" },
      { id: "d", text: "The p99 latency of the inference endpoint in milliseconds" },
      { id: "e", text: "GPU utilization across the inference fleet" },
      { id: "f", text: "The model's score on a held-out summarization benchmark" },
      { id: "g", text: "The number of Provisioned Throughput model units purchased" },
      { id: "h", text: "Purchase conversion rate for chats handled by the assistant" },
      { id: "i", text: "Support cost saved per conversation the assistant resolved" },
      { id: "j", text: "Average time to first token for each response" },
    ],
    correctOptionIds: ["a", "b", "h", "i"],
    answerType: "multi",
    explanation: "Business value metrics for generative AI include user satisfaction, conversion rate, average revenue per user, and efficiency or cost savings; token counts, latency, hardware utilization, benchmark scores, and purchased capacity are operational and model metrics that support those outcomes without replacing them.",
    optionRationale: {
      a: "Customer satisfaction measures the outcome the assistant exists to improve.",
      b: "Average revenue per user is a standard business value metric for a customer-facing feature.",
      c: "Tokens per request is a cost and efficiency measure of the implementation.",
      d: "Endpoint latency is an operational service level measure.",
      e: "GPU utilization describes infrastructure efficiency.",
      f: "A benchmark score measures model quality rather than business outcome.",
      g: "Purchased model units are a capacity and cost input, not a result.",
      h: "Conversion rate shows whether assisted customers go on to buy, which is a direct measure of business value.",
      i: "Cost savings from conversations resolved without a human agent are a standard business value metric for an assistant.",
      j: "Time to first token is a latency measure of the serving stack, not a business outcome.",
    },
    referenceUrl: "https://docs.aws.amazon.com/wellarchitected/latest/machine-learning-lens/machine-learning-lens.html",
    referenceLabel: "AWS Well-Architected Machine Learning Lens",
  },
  {
    id: "aif-gen25",
    domain: "genai-fundamentals",
    text: "An interactive feature sorts short support messages into one of eight categories, must answer within about a second, and will handle 30 million messages a month. The team prototyped with the largest and most capable model on Amazon Bedrock and is now worried about cost and latency. What is the best first step?",
    options: [
      { id: "a", text: "Test a smaller, lower-cost model on the same task and keep it if its accuracy is good enough, because smaller models generally cost less per token and respond faster" },
      { id: "b", text: "Keep the largest model, because a larger model is always more accurate on every task" },
      { id: "c", text: "Move the traffic to batch inference, even though each message needs an immediate answer" },
      { id: "d", text: "Increase the maximum response length so that fewer calls are needed" },
      { id: "e", text: "Raise the temperature so that the responses come back shorter" },
      { id: "f", text: "Send all 30 million messages in one prompt to a model with a very large context window" },
      { id: "g", text: "Self-host the largest model on GPU instances, which is always cheaper than a managed API" },
      { id: "h", text: "Run an Amazon Bedrock model evaluation job comparing smaller models with the prototype on labeled sample messages" },
    ],
    correctOptionIds: ["a", "h"],
    answerType: "single",
    explanation: "Model choice is a trade-off between quality, latency, and price per token, and a short, well-defined classification task rarely needs the most capable model; the standard practice is to try a smaller and cheaper model against a representative test set and move up only if quality falls short.",
    optionRationale: {
      a: "Right-sizing the model to the task is the normal way to cut both cost and latency without harming outcomes.",
      b: "Larger models cost more and respond more slowly, and simple tasks often do not benefit from them.",
      c: "Batch inference lowers price by relaxing latency, which this interactive requirement forbids.",
      d: "A longer allowed response can increase output tokens and cost, and does not reduce the number of messages.",
      e: "Temperature affects randomness, not response length or price.",
      f: "One giant prompt would exceed any context window and would not return per-message answers.",
      g: "Self-hosting adds GPU capacity, operations, and idle time costs, and is not automatically cheaper.",
      h: "Evaluating cheaper, faster models against the prototype on representative labeled data shows whether a smaller model is accurate enough before switching.",
    },
    referenceUrl: "https://aws.amazon.com/bedrock/pricing/",
    referenceLabel: "Amazon Bedrock pricing",
    consoleUrl: "https://console.aws.amazon.com/bedrock/",
    consoleLabel: "Amazon Bedrock console",
  },
  {
    id: "aif-gen26",
    domain: "genai-fundamentals",
    text: "A legal publisher is preparing 30,000 long court judgments for semantic search and Retrieval Augmented Generation. Each judgment is split into chunks, and each chunk is embedded separately. The team is now debating how large the chunks should be. Which statement about chunk size is accurate?",
    options: [
      { id: "a", text: "Chunk size changes only how much the vector store costs; retrieval quality and answer quality are the same at any size" },
      { id: "b", text: "Oversized chunks blend several topics into one vector and waste prompt space, while tiny chunks lose the context a passage needs" },
      { id: "c", text: "Each whole judgment should be a single chunk, because the larger a chunk is, the more precisely its single vector matches any question a user asks" },
      { id: "d", text: "Each sentence should be its own chunk, because the smallest possible chunks always give the most relevant and the most complete answers" },
      { id: "e", text: "Chunk size controls the temperature the model uses when it writes the final answer, so it should match the desired creativity" },
      { id: "f", text: "Chunking is only a compression step that shrinks the files so the vector store bills less per gigabyte stored" },
      { id: "g", text: "Smaller chunks match a question precisely but carry less context; larger chunks keep context but match less precisely" },
      { id: "h", text: "Once documents are chunked, the chunks can be matched to questions directly, so embeddings are no longer needed" },
      { id: "i", text: "Overlap between neighboring chunks should always be avoided, because text repeated across boundaries never helps retrieval" },
    ],
    correctOptionIds: ["b", "g"],
    answerType: "single",
    explanation: "Long documents are split into chunks so that each focused passage gets its own embedding, fits the embedding model's input limit, and can be retrieved on its own. Chunk size is a trade-off: small chunks give precise embeddings but can lose the context needed to understand them, while large chunks keep context but merge several ideas into one vector, which weakens the match, and they use more of the prompt when retrieved. Overlap between chunks and strategies such as hierarchical chunking exist to balance the two.",
    optionRationale: {
      a: "Chunk size shapes what each vector represents and how much text reaches the model, so it affects retrieval and answer quality, not just storage.",
      b: "An oversized chunk averages several ideas into one vector and fills the prompt with text the question does not need, and a tiny chunk can be stripped of the context that gives it meaning.",
      c: "A whole long judgment in one vector blends many topics, so its embedding matches any single question weakly, and retrieving it sends far more text than needed.",
      d: "Sentence-sized chunks often lose the surrounding facts needed to interpret them, so they can match precisely yet leave the model without enough context.",
      e: "Temperature is an inference parameter set on the generation request; chunk size has no effect on it.",
      f: "Chunking exists so retrieval can work on focused passages that fit the embedding model's input limit, not to compress files for storage billing.",
      g: "This is the core trade-off between a precise match and complete context, with larger chunks also consuming more of the prompt's token budget.",
      h: "Chunks still have to be embedded, because the embeddings are what semantic retrieval compares with the embedded question.",
      i: "A modest overlap is a common way to keep ideas that straddle a chunk boundary intact, so it often helps.",
    },
    referenceUrl: "https://docs.aws.amazon.com/bedrock/latest/userguide/kb-chunking.html",
    referenceLabel: "How content chunking works for knowledge bases",
  },
  {
    id: "aif-gen27",
    domain: "genai-fundamentals",
    text: "A developer adds top-k = 50 to the inference parameters of requests sent to a text model on Amazon Bedrock. What effect does this setting have?",
    options: [
      { id: "a", text: "The response is limited to at most 50 output tokens" },
      { id: "b", text: "The model keeps the smallest set of next tokens whose probabilities add up to 50 percent" },
      { id: "c", text: "At each step, the next token is sampled only from the 50 most probable candidates" },
      { id: "d", text: "The model returns 50 alternative responses and the application picks one of them" },
      { id: "e", text: "The probability distribution is flattened so that unlikely tokens are picked more often" },
      { id: "f", text: "Generation stops as soon as the model has written 50 sentences" },
      { id: "g", text: "The 50 most relevant document chunks are retrieved and added to the prompt" },
      { id: "h", text: "Before each token is chosen, every candidate outside the 50 highest-probability options is discarded" },
      { id: "i", text: "The request is billed for no more than 50 input tokens, whatever the prompt length" },
    ],
    correctOptionIds: ["c", "h"],
    answerType: "single",
    explanation: "Top-k limits the candidates for each next token to a fixed number of the most likely ones before sampling: a lower value keeps output to more predictable tokens, and a higher value lets less likely tokens through. Top-p instead keeps a variable number of candidates whose cumulative probability reaches a threshold, temperature reshapes the whole distribution, and the maximum-token setting limits length.",
    optionRationale: {
      a: "Output length is capped by the maximum-tokens setting; top-k limits which tokens are considered at each step, not how many are produced.",
      b: "Keeping candidates up to a cumulative probability is top-p, also called nucleus sampling, not top-k.",
      c: "Top-k restricts sampling to a fixed count of the most likely next tokens, here the top 50.",
      d: "Top-k shapes a single response token by token; it does not generate several candidate responses.",
      e: "Flattening the distribution is what a higher temperature does; top-k truncates the candidate list instead of reshaping it.",
      f: "Top-k has nothing to do with sentences; length and stopping are governed by maximum tokens and stop sequences.",
      g: "The number of passages returned is a retrieval setting of a knowledge base, not the top-k sampling parameter of a text model.",
      h: "Discarding all but the k most probable candidates before sampling is exactly how top-k narrows the choice.",
      i: "Input tokens are billed in full whatever the sampling settings; top-k does not change how the prompt is metered.",
    },
    referenceUrl: "https://docs.aws.amazon.com/bedrock/latest/userguide/inference-parameters.html",
    referenceLabel: "Influence response generation with inference parameters",
    consoleUrl: "https://console.aws.amazon.com/bedrock/",
    consoleLabel: "Amazon Bedrock console",
  },
  {
    id: "aif-gen28",
    domain: "genai-fundamentals",
    text: "A team uses a text model to generate sample dialogues between customers and support agents. Each prompt ends with \"Agent:\", but after writing the agent's reply the model keeps going and invents the customer's next line, which starts with \"Customer:\". Which inference setting makes generation end right after the agent's reply?",
    options: [
      { id: "a", text: "Lower the temperature so the model writes less creatively" },
      { id: "b", text: "Raise top-p so that more candidate tokens are considered" },
      { id: "c", text: "Set top-k to 1 so the model always picks the most likely token" },
      { id: "d", text: "Set \"Customer:\" as a stop sequence" },
      { id: "e", text: "Raise the maximum output tokens so the reply is never cut short" },
      { id: "f", text: "Choose a model with a larger context window" },
      { id: "g", text: "Cap the maximum output tokens at a fixed number such as 60" },
      { id: "h", text: "Turn on response streaming so the reply arrives sooner" },
      { id: "i", text: "Supply a string that ends generation as soon as the model writes it, here the label that opens the customer's turn" },
    ],
    correctOptionIds: ["d", "i"],
    answerType: "single",
    explanation: "Stop sequences are strings that end generation when the model produces them. Using the label that opens the next speaker's turn, or an end marker the prompt asks the model to write, cuts the output at the right point however long the reply is. Temperature, top-p, and top-k change word choice, and the maximum-token setting only caps length.",
    optionRationale: {
      a: "Temperature changes how predictable the wording is; the model can still continue into an invented customer line.",
      b: "A higher top-p widens the candidate pool and does nothing to end generation at the turn boundary.",
      c: "Always picking the most likely token makes the wording repeatable but does not stop the model from writing the next speaker's line.",
      d: "Generation ends as soon as the model produces the stop sequence, so the output stops before the invented customer line.",
      e: "More output tokens give the model more room to keep writing, which makes the problem worse.",
      f: "A larger context window allows longer input and does not tell the model where to stop.",
      g: "Replies vary in length, so a fixed cap either cuts some replies mid-sentence or still leaves room for the invented line.",
      h: "Streaming changes how quickly text is delivered, not where generation ends.",
      i: "That is a stop sequence: a string such as \"Customer:\" whose appearance ends generation exactly at the turn boundary.",
    },
    referenceUrl: "https://docs.aws.amazon.com/bedrock/latest/userguide/inference-parameters.html",
    referenceLabel: "Influence response generation with inference parameters",
    consoleUrl: "https://console.aws.amazon.com/bedrock/",
    consoleLabel: "Amazon Bedrock console",
  },
  {
    id: "aif-gen29",
    domain: "genai-fundamentals",
    text: "A startup founder assumes that pre-training a large language model from scratch requires millions of examples labeled by human annotators. How are such models actually pre-trained?",
    options: [
      { id: "a", text: "Through supervised learning on millions of question-and-answer pairs that teams of human annotators write and label by hand" },
      { id: "b", text: "Through reinforcement learning in which human raters score every sentence from the very first training step" },
      { id: "c", text: "With labels taken from the text itself: each next word in the corpus is the target, so no manual labeling is needed" },
      { id: "d", text: "Through unsupervised clustering that groups similar documents, with no prediction task involved" },
      { id: "e", text: "By encoding hand-written grammar rules that the model then follows when it generates text" },
      { id: "f", text: "Through self-supervised learning on huge unlabeled text corpora, typically by predicting the next token" },
      { id: "g", text: "By storing the whole corpus in a database and retrieving the closest sentence for each prompt" },
      { id: "h", text: "By training a separate small model for each task the finished model will later perform" },
      { id: "i", text: "Through transfer learning from a small labeled sample of the startup's own documents" },
    ],
    correctOptionIds: ["c", "f"],
    answerType: "single",
    explanation: "Foundation models are pre-trained with self-supervised learning: the model reads huge unlabeled corpora and learns by predicting the next token, or a masked one, so the data provides its own labels. Human-labeled data comes later and in much smaller amounts, for fine-tuning and alignment steps such as instruction tuning and reinforcement learning from human feedback.",
    optionRationale: {
      a: "Labeled prompt-and-response pairs are used later, in instruction fine-tuning, and in far smaller numbers than the pre-training corpus.",
      b: "Reinforcement learning from human feedback is an alignment step applied after pre-training, not how the base model first learns language.",
      c: "Because the next word of real text serves as the target, the corpus labels itself, which is what makes training on web-scale text feasible.",
      d: "Pre-training is a prediction task in which the model learns by predicting next or missing tokens, not by clustering documents.",
      e: "Language models learn statistical patterns from data; no one hand-writes grammar rules for them.",
      f: "Foundation models are pre-trained with self-supervised learning, which creates labels from the input data itself, typically through next-token prediction.",
      g: "A model learns patterns into its parameters rather than storing a retrievable copy of the corpus.",
      h: "One pre-trained foundation model serves many tasks; building a model per task is the traditional ML approach it replaces.",
      i: "A small company sample could be used later for customization, but pre-training uses very large, broad corpora.",
    },
    referenceUrl: "https://aws.amazon.com/what-is/foundation-models/",
    referenceLabel: "What are foundation models? (AWS)",
  },
  {
    id: "aif-gen30",
    domain: "genai-fundamentals",
    text: "A telecom support portal uses keyword search. A customer types \"my phone keeps dropping calls\" and gets no results, although an article titled \"Fixing intermittent voice disconnections\" answers the question exactly. Which approach would find that article?",
    options: [
      { id: "a", text: "Exact string matching on article titles, which is stricter than keyword search and therefore surfaces more relevant articles" },
      { id: "b", text: "Ranking articles by how many letters they share with the query, which catches misspellings and near-miss words as well" },
      { id: "c", text: "Raising the temperature of a text generation model so that it considers a wider and more varied set of articles" },
      { id: "d", text: "Comparing the query's embedding with each article's embedding and returning the nearest vectors, since closeness reflects meaning" },
      { id: "e", text: "Representing each article as a one-hot encoding of its title words, which captures how similar two texts are in their meaning" },
      { id: "f", text: "Sorting the keyword results by publication date so that the newest and most relevant articles appear at the top" },
      { id: "g", text: "Semantic search, which ranks articles by the cosine similarity of their embeddings to the query's embedding" },
      { id: "h", text: "Searching only the article titles instead of the full article text, which removes irrelevant matches from the results" },
      { id: "i", text: "Choosing a model with a larger context window so that longer customer questions fit into the search request" },
    ],
    correctOptionIds: ["d", "g"],
    answerType: "single",
    explanation: "Keyword (lexical) search matches the words a user types, so it misses relevant content that is phrased differently. Semantic search embeds the query and the documents with the same embedding model and ranks documents by a similarity measure such as cosine similarity or vector distance, so text with the same meaning is found even when no words are shared. Many search systems combine both approaches as hybrid search.",
    optionRationale: {
      a: "Exact matching is stricter than keyword search, so it finds even less when the customer and the article use different words.",
      b: "Shared letters measure spelling, not meaning, and \"dropping calls\" and \"voice disconnections\" have little in common letter by letter.",
      c: "Temperature affects how a generative model samples tokens and plays no part in retrieving documents.",
      d: "Embedding both texts and returning the nearest vectors retrieves articles by meaning, so different wording still matches.",
      e: "One-hot encodings are sparse and carry no notion of similarity, so they cannot match different words with the same meaning.",
      f: "Sorting changes the order of results that were found and cannot surface an article that keyword matching missed.",
      g: "Semantic search compares meaning through embeddings and a similarity measure such as cosine similarity, which is exactly what this wording mismatch needs.",
      h: "Narrowing the search to titles removes candidate matches rather than adding the one that uses different words.",
      i: "The query is already short, and context window size does not change whether keyword matching finds the article.",
    },
    referenceUrl: "https://docs.aws.amazon.com/ai/responsible-ai/titan-text-embeddings/overview.html",
    referenceLabel: "Amazon Titan Text Embeddings (AWS AI Service Card)",
  },
  {
    id: "aif-gen31",
    domain: "genai-fundamentals",
    text: "A news publisher wants a \"more like this\" panel that shows stories similar to the one a reader is viewing, and wants to group 2 million archived stories into topic clusters. A developer proposes prompting a large text generation model to compare stories. Which kind of model is the better fit for these features?",
    options: [
      { id: "a", text: "A large text generation model prompted to compare every pair of stories and reply with a similarity score from 1 to 10" },
      { id: "b", text: "An embedding model such as Amazon Titan Text Embeddings V2, which turns each story into a comparable vector" },
      { id: "c", text: "A text-to-image diffusion model that illustrates each story so that the pictures can be compared" },
      { id: "d", text: "A speech-to-text model that transcribes each story before the stories are compared" },
      { id: "e", text: "A text-to-speech model that reads each story aloud so that listeners can judge similarity" },
      { id: "f", text: "A time series forecasting model trained on each story's daily page views" },
      { id: "g", text: "An image classification model that assigns a category label to each story's lead photo" },
      { id: "h", text: "A text generation model with temperature set to zero, whose summaries of the stories are then compared word by word" },
      { id: "i", text: "A text embedding model such as Cohere Embed on Amazon Bedrock, whose vectors can be clustered and searched" },
    ],
    correctOptionIds: ["b", "i"],
    answerType: "single",
    explanation: "Similarity search, recommendations, and clustering all run on embeddings: an embedding model maps each item to a vector so that related items sit close together, and those vectors can be indexed, compared, and clustered cheaply at scale. A text generation model produces new text rather than comparable vectors, so it is the wrong tool for comparing millions of items. Amazon Titan Text Embeddings V2 and the Cohere Embed models on Amazon Bedrock are built for these tasks.",
    optionRationale: {
      a: "Comparing 2 million stories pair by pair would take trillions of slow, costly generation calls, and generated scores are not consistent enough to rank or cluster reliably.",
      b: "Titan Text Embeddings V2 produces vectors for retrieval that also serve semantic similarity and clustering, which is exactly what both features need.",
      c: "A diffusion model generates images, and picture similarity says little about whether two stories cover the same topic.",
      d: "The stories are already text, and transcription produces text rather than a representation that can be compared.",
      e: "Text-to-speech creates audio for listening and provides nothing that can be compared or clustered.",
      f: "Forecasting predicts future values of a time series such as page views, not how similar two stories are.",
      g: "A label for the lead photo ignores the story's text and is far too coarse for finding similar stories.",
      h: "Comparing summaries word by word is keyword matching in disguise and misses stories that say the same thing in different words.",
      i: "Cohere Embed models on Amazon Bedrock produce embeddings for search, classification, and clustering, so similar stories can be found by vector distance.",
    },
    referenceUrl: "https://docs.aws.amazon.com/bedrock/latest/userguide/titan-embedding-models.html",
    referenceLabel: "Amazon Titan Text Embeddings models",
    consoleUrl: "https://console.aws.amazon.com/bedrock/",
    consoleLabel: "Amazon Bedrock console",
  },
  {
    id: "aif-gen32",
    domain: "genai-fundamentals",
    text: "A furniture retailer has 400,000 product photos with no captions or descriptions, and the file names are internal stock numbers. It wants shoppers to type a phrase such as \"mid-century walnut armchair\" and see matching photos. Which approach fits best?",
    options: [
      { id: "a", text: "Embed the query with a text-only embedding model and the photos with an unrelated image model, then compare the two vectors directly" },
      { id: "b", text: "Generate a new picture from each query with a text-to-image model and show it instead of the catalog photos" },
      { id: "c", text: "Run keyword search on the photo file names, then show the photos whose names contain the typed words" },
      { id: "d", text: "Extract any printed text from each photo with Amazon Textract, then search that text for the typed words" },
      { id: "e", text: "Embed the photos and each typed query with a multimodal embedding model such as Amazon Nova Multimodal Embeddings, then return the closest photos" },
      { id: "f", text: "Raise top-k on a text generation model so that it considers more photos before it answers" },
      { id: "g", text: "Encode each photo as a one-hot vector of its pixel colors and match the typed words against those vectors" },
      { id: "h", text: "Use Cohere Embed v4 on Amazon Bedrock, which places text and images in one vector space, and rank photos by similarity to the query" },
      { id: "i", text: "Fine-tune a text generation model on the stock numbers so that it memorizes the whole catalog" },
    ],
    correctOptionIds: ["e", "h"],
    answerType: "single",
    explanation: "Multimodal embedding models map text and images into a shared vector space, so a written description and a photo of the same thing get nearby vectors. The catalog photos are embedded once and stored in a vector index, each query is embedded at search time with the same model, and the nearest image vectors are returned. On Amazon Bedrock, Amazon Nova Multimodal Embeddings, Cohere Embed v4, and Amazon Titan Multimodal Embeddings G1 support this kind of cross-modal search.",
    optionRationale: {
      a: "Vectors from two unrelated models live in different spaces, so the distance between a text vector and an image vector means nothing.",
      b: "Generating a new image shows the shopper something that may not exist in the catalog instead of finding real products.",
      c: "The file names are stock numbers, so the typed words never match them.",
      d: "Furniture photos contain little or no printed text, so there is nothing useful for Textract to extract.",
      e: "A multimodal embedding model puts text and images in one shared space, so the vectors of a description and a matching photo end up close together.",
      f: "Top-k tunes token sampling in text generation; it neither reads photos nor searches a catalog.",
      g: "One-hot pixel colors carry no meaning and cannot be compared with words.",
      h: "Cohere Embed v4 embeds both text and images into a single vector space, which makes text-to-image search possible.",
      i: "A generation model trained on stock numbers learns nothing about how the furniture looks and does not search images.",
    },
    referenceUrl: "https://docs.aws.amazon.com/nova/latest/nova2-userguide/embeddings.html",
    referenceLabel: "Using Amazon Nova embeddings",
    consoleUrl: "https://console.aws.amazon.com/bedrock/",
    consoleLabel: "Amazon Bedrock console",
  },
  {
    id: "aif-gen33",
    domain: "genai-fundamentals",
    text: "An airline wants a phone assistant that listens to callers and answers aloud in real time, copes naturally when a caller interrupts, and runs on one foundation model rather than a chain of separate transcription, text generation, and speech synthesis services. Which option fits best?",
    options: [
      { id: "a", text: "Amazon Polly, which converts written text into lifelike speech in many voices" },
      { id: "b", text: "Amazon Transcribe, which converts recorded or streamed speech into text" },
      { id: "c", text: "Amazon Nova 2 Sonic, a speech-to-speech foundation model on Amazon Bedrock" },
      { id: "d", text: "A text-to-image diffusion model that turns each spoken request into a picture" },
      { id: "e", text: "An embedding model such as Amazon Titan Text Embeddings V2, which returns vectors" },
      { id: "f", text: "Amazon Translate, which converts text from one language into another" },
      { id: "g", text: "A speech-to-speech foundation model that understands spoken input and replies with generated speech over a bidirectional stream" },
      { id: "h", text: "A text-only large language model that accepts typed prompts and returns text" },
      { id: "i", text: "Amazon Comprehend, which detects the sentiment and key phrases in text" },
    ],
    correctOptionIds: ["c", "g"],
    answerType: "single",
    explanation: "Generative AI is not limited to text: speech-to-speech foundation models understand spoken input and generate spoken output in a single model, streaming audio in both directions so a voice assistant can reply in real time, take turns naturally, and handle interruptions. Amazon Nova 2 Sonic on Amazon Bedrock is such a model. Chaining Amazon Transcribe, a text model, and Amazon Polly also works, but every extra hop adds latency and integration work.",
    optionRationale: {
      a: "Polly only speaks text it is given; it neither understands the caller nor decides what to say, so it would be just one link in a chain.",
      b: "Transcribe turns speech into text but does not generate an answer or speak it.",
      c: "Nova 2 Sonic unifies speech understanding and generation in one model, streaming audio both ways with natural turn-taking and graceful handling of interruptions.",
      d: "A diffusion model generates images, which is no use for a spoken conversation.",
      e: "An embedding model returns vectors for search and comparison, not spoken replies.",
      f: "Translate converts text between languages and neither listens nor speaks.",
      g: "A speech-to-speech model takes the caller's audio directly and streams back a spoken reply, which avoids chaining separate services.",
      h: "A text-only model cannot take the caller's audio or reply aloud without other services wrapped around it.",
      i: "Comprehend analyzes text; it does not hold a spoken conversation.",
    },
    referenceUrl: "https://docs.aws.amazon.com/nova/latest/nova2-userguide/using-conversational-speech.html",
    referenceLabel: "Speech-to-Speech (Amazon Nova 2 Sonic)",
    consoleUrl: "https://console.aws.amazon.com/bedrock/",
    consoleLabel: "Amazon Bedrock console",
  },
  {
    id: "aif-gen34",
    domain: "genai-fundamentals",
    text: "An operations director asks why the company should use generative AI for customer communications instead of building a separate traditional ML model for each task. Which TWO are genuine advantages of generative AI?",
    options: [
      { id: "a", text: "Accuracy: its answers are guaranteed to be factually correct, so generated outputs never need human review" },
      { id: "b", text: "Adaptability: one foundation model can summarize, translate, classify, and draft, switching tasks through the prompt alone" },
      { id: "c", text: "Determinism: identical prompts always produce identical output, which keeps testing simple" },
      { id: "d", text: "Transparency: the exact reason behind every output can be traced step by step, which makes compliance audits simple" },
      { id: "e", text: "Responsiveness: it produces tailored content in real time, within seconds of each request" },
      { id: "f", text: "Currency: it knows about events after its training cutoff without being given them" },
      { id: "g", text: "Autonomy: it removes the need for human oversight of regulated customer decisions" },
      { id: "h", text: "Simplicity: staff instruct it in plain natural language, with no task-specific training or feature engineering" },
      { id: "i", text: "Fairness: it cannot reflect bias, because it learns from very large datasets" },
      { id: "j", text: "Personalization: it can tailor each message to the details of the individual customer's request" },
      { id: "k", text: "Zero marginal cost: once a model is chosen, generating content incurs no further charges" },
    ],
    correctOptionIds: ["b", "e", "h", "j"],
    answerType: "multi",
    explanation: "Generative AI's advantages come from foundation models: adaptability (one pre-trained model performs many tasks based on the prompt), responsiveness (content generated in real time for each request), simplicity (a plain-language interface with no task-specific training), and personalization of each output. It does not guarantee accuracy, determinism, interpretability, current knowledge, fairness, or free operation, which is why designs still include grounding, evaluation, and human review.",
    optionRationale: {
      a: "Generative models can produce confident but false content, so output still needs grounding, evaluation, and review.",
      b: "A foundation model handles many different tasks based on the prompt, whereas traditional ML needs a separately trained model for each task.",
      c: "Generation samples from a probability distribution, so wording varies between calls unless decoding is tightly constrained.",
      d: "Large models are hard to interpret, and the exact reason for a given output cannot be traced.",
      e: "Content is generated on demand within seconds for each request, which suits live customer interactions.",
      f: "A model knows nothing after its training cutoff unless the application supplies the information.",
      g: "Regulated decisions still need accountable human oversight; generative AI does not remove that requirement.",
      h: "A natural language interface lets people direct the model without engineering features or training a task-specific model.",
      i: "Models absorb biases present in their training data, so outputs must be evaluated for fairness.",
      j: "Because each response is generated for the specific request, messages can be personalized to the customer's situation.",
      k: "Inference is billed per token or per unit of reserved capacity, so every generated response has a cost.",
    },
    referenceUrl: "https://aws.amazon.com/what-is/foundation-models/",
    referenceLabel: "What are foundation models? (AWS)",
  },
  {
    id: "aif-gen35",
    domain: "genai-fundamentals",
    text: "A hospital uses a large language model to draft discharge summaries that physicians review and sign. After one draft leaves out a documented allergy, the quality team asks for an exact explanation of why the model dropped that detail. What is the most accurate response?",
    options: [
      { id: "a", text: "Export the model's decision rules, which list the exact conditions under which each clinical detail is included in a summary" },
      { id: "b", text: "Ask the model why it left the allergy out, because its reply is an exact record of the internal computation that produced the draft" },
      { id: "c", text: "Set the temperature to zero, which turns the model's internal reasoning into readable rules" },
      { id: "d", text: "No exact account exists: the output emerges from billions of learned weights, so the reason for one omission cannot be traced" },
      { id: "e", text: "Read the model's weights directly, because each weight corresponds to one clinical instruction" },
      { id: "f", text: "Pull the model invocation logs, which record the step-by-step reasoning performed inside each layer of the model for every request" },
      { id: "g", text: "Switch to a model with a larger context window, which lists its reasons for every omission in the output" },
      { id: "h", text: "Fine-tune the model on past summaries, after which it reports why it includes or omits each detail" },
      { id: "i", text: "The model is a black box, so the team should rely on evidence it can inspect, such as the logged input and output and physician review" },
    ],
    correctOptionIds: ["d", "i"],
    answerType: "single",
    explanation: "Large language models are often described as black boxes: an output emerges from interactions among billions of learned parameters, and understanding how the model arrived at a specific output is challenging. When a result must be explained exactly, teams rely on evidence they can inspect, such as logged prompts and responses, the source passages the model was given, and accountable human review, and they keep decisions that need an exact rationale in transparent rules or interpretable models.",
    optionRationale: {
      a: "A large language model has no explicit rule set to export; its behavior is spread across its learned parameters.",
      b: "A self-explanation is newly generated text that can sound convincing and still be wrong; it is not a record of the model's internal computation.",
      c: "Temperature changes how tokens are sampled, and a repeatable output is still produced by the same opaque computation.",
      d: "Understanding how a large model arrived at a specific output is inherently hard, so a precise causal account of one omission cannot be produced.",
      e: "Individual weights do not map to readable instructions, because what the model has learned is distributed across billions of them.",
      f: "Invocation logs capture the prompts and responses exchanged with the model, not what happens inside its layers.",
      g: "A larger context window lets more text in; it does not make the model explain itself.",
      h: "Fine-tuning changes the model's behavior but does not make its internal reasoning traceable.",
      i: "Because the model's reasoning cannot be traced, accountability rests on evidence the team can inspect and on the physician who signs the summary.",
    },
    referenceUrl: "https://aws.amazon.com/what-is/generative-ai/",
    referenceLabel: "What is generative AI? (AWS)",
  },
  {
    id: "aif-gen36",
    domain: "genai-fundamentals",
    text: "An online travel agency estimated its Amazon Bedrock bill by counting the words in its prompts and assuming one token per word. Actual input token counts came in higher, higher still for customer messages in Hindi and Arabic, and different again when the team tested a model from another provider. Which conclusion should the team draw?",
    options: [
      { id: "a", text: "Amazon Bedrock bills per word, so the higher token counts must be a metering error to raise with AWS" },
      { id: "b", text: "All models share one tokenizer, so the difference between the two providers must come from the temperature setting in each request" },
      { id: "c", text: "Only output tokens are billed, so the length of the prompt has no effect on the monthly bill" },
      { id: "d", text: "Text in other languages is translated into English before it is tokenized, so the language of a message cannot change its token count" },
      { id: "e", text: "Tokens are not words: each model's tokenizer splits text its own way, and some languages need more tokens per word" },
      { id: "f", text: "Context limits are counted in words, so the mismatch affects the price of each request but never the maximum input size" },
      { id: "g", text: "A token is always exactly four characters, so the count depends only on the length of the text" },
      { id: "h", text: "Estimate with the target model, for example the Amazon Bedrock CountTokens API, because token counts differ between tokenizers" },
      { id: "i", text: "Choosing a model with a larger context window makes each word count as a single token" },
    ],
    correctOptionIds: ["e", "h"],
    answerType: "single",
    explanation: "Models read and bill text in tokens, and a token is often a piece of a word rather than a whole one. Each model's tokenizer splits text its own way, so the same prompt can produce different token counts on different models, and many languages need more tokens per word than English. Prices, context windows, and most quotas are measured in tokens, so estimates should come from counting tokens with the target model, for example with the Amazon Bedrock CountTokens API.",
    optionRationale: {
      a: "On-demand text inference is metered in input and output tokens, not words, so the difference is expected rather than an error.",
      b: "Tokenization is model-specific, and temperature affects sampling, not how input text is counted.",
      c: "Input tokens are billed as well as output tokens, so long prompts raise the bill.",
      d: "Text is tokenized as sent, and many tokenizers split non-English or non-Latin-script text into more tokens per word.",
      e: "A token is often a fragment of a word, the split depends on each model's tokenizer, and some languages need more tokens per word, so words are a poor proxy.",
      f: "Context windows are also measured in tokens, so a higher token count can push a request past the model's input limit.",
      g: "Characters per token vary with the text, the language, and the tokenizer; about four characters is only a rough average for English.",
      h: "CountTokens returns the input token count the chosen model would charge for, which is a reliable basis for cost and limit planning.",
      i: "The context window limits how many tokens fit in a request; it does not change how the tokenizer splits words.",
    },
    referenceUrl: "https://docs.aws.amazon.com/bedrock/latest/userguide/count-tokens.html",
    referenceLabel: "Monitor your token usage by counting tokens before running inference",
    consoleUrl: "https://console.aws.amazon.com/bedrock/",
    consoleLabel: "Amazon Bedrock console",
  },
  {
    id: "aif-gen37",
    domain: "genai-fundamentals",
    text: "A Canadian health insurer is choosing a foundation model for a claims assistant. Claim data may be processed only in the Canada (Central) Region, one candidate is an open weights model the team would host itself, the assistant must read photos of paper receipts, and agents expect replies within two seconds. Which TWO checks belong in the model selection?",
    options: [
      { id: "a", text: "Confirm that each managed candidate model can be used in the Canada (Central) Region, so claim data stays there" },
      { id: "b", text: "Choose the model with the most parameters, because the largest model is always the safest compliance choice" },
      { id: "c", text: "Skip the Region check, because every Amazon Bedrock model is offered in every AWS Region" },
      { id: "d", text: "Review the open weights model's license terms to confirm they allow the insurer's commercial use" },
      { id: "e", text: "Skip the license review, because open weights models are free of any usage terms" },
      { id: "f", text: "Rank candidates only by a public leaderboard score, which removes the need to test on the insurer's own claims" },
      { id: "g", text: "Prefer the newest model release, because newer models are automatically approved for health data" },
      { id: "h", text: "Confirm the model accepts image input, so it can read the photographed receipts" },
      { id: "i", text: "Pick the model whose provider is headquartered in Canada, because that decides where inference runs" },
      { id: "j", text: "Choose the model with the largest context window, because a larger window also means faster replies" },
      { id: "k", text: "Measure each candidate's response time on realistic claims against the two-second target" },
    ],
    correctOptionIds: ["a", "d", "h", "k"],
    answerType: "multi",
    explanation: "Model selection starts from the workload's constraints: compliance (where data may be processed, which rules out models not offered in the required Region, and whether an open weights license permits the intended use), capabilities (input and output modalities such as images), and performance (latency measured on realistic requests), alongside cost. Size, novelty, leaderboard rank, and the provider's home country do not substitute for checking these requirements.",
    optionRationale: {
      a: "Data residency is a compliance constraint, and model availability varies by Region, so each candidate must be usable where the data is allowed to be processed.",
      b: "Parameter count says nothing about compliance, and larger models are usually slower and more expensive.",
      c: "Model availability differs by Region, so this check is essential rather than optional.",
      d: "Open weights models are released under licenses that can restrict use, and the customer is responsible for confirming the terms fit its use case.",
      e: "Open weights models come with license terms, and some of them restrict commercial use.",
      f: "Public benchmarks rarely reflect a specific workload, so candidates still need to be evaluated on representative claims.",
      g: "No model is automatically approved for regulated data; compliance depends on how and where it is deployed and used.",
      h: "Reading receipts needs a model that accepts images, so input modality is a hard requirement.",
      i: "Inference runs in the AWS Region or infrastructure the customer chooses, not in the provider's home country.",
      j: "Context window size and speed are separate characteristics, and a larger window does not make a model respond faster.",
      k: "A latency requirement has to be verified by measuring response times on realistic requests, because models differ widely in speed.",
    },
    referenceUrl: "https://docs.aws.amazon.com/bedrock/latest/userguide/models.html",
    referenceLabel: "Model availability & compatibility (Amazon Bedrock)",
    consoleUrl: "https://console.aws.amazon.com/bedrock/",
    consoleLabel: "Amazon Bedrock console",
  },
  {
    id: "aif-gen38",
    domain: "genai-fundamentals",
    text: "A news publisher wants a two-sentence summary and five topic tags for each of 3 million archived articles, generated by a foundation model on Amazon Bedrock. Nobody is waiting on the results, which are due within a week. The team wants a lower model price than on-demand invocation and does not want to write code that sends millions of separate requests. Which approach fits best?",
    options: [
      { id: "a", text: "Amazon Bedrock batch inference, with the prompts in JSONL files in Amazon S3 and the results written back to S3" },
      { id: "b", text: "Buy Provisioned Throughput for the model with a six-month commitment and run the archive through it" },
      { id: "c", text: "Loop over the articles and call the InvokeModel API on demand for each one as fast as quotas allow" },
      { id: "d", text: "Send every request on the Priority service tier so that the whole archive finishes sooner" },
      { id: "e", text: "Call the ConverseStream API so that each summary streams back at a lower price per token" },
      { id: "f", text: "Index the articles in an Amazon Bedrock knowledge base, which writes a summary of each document as it is ingested" },
      { id: "g", text: "Deploy a model to an Amazon SageMaker AI real-time endpoint and keep it running until every article is done" },
      { id: "h", text: "Submit the articles as an asynchronous batch inference job, priced lower than on-demand for supported models" },
      { id: "i", text: "Switch to the largest available model, which charges less per token for long documents" },
    ],
    correctOptionIds: ["a", "h"],
    answerType: "single",
    explanation: "Amazon Bedrock batch inference suits large offline jobs: the prompts go into files in Amazon S3, one job request processes them asynchronously, and the responses are written back to S3. Because nobody waits on individual answers, supported models are offered for batch at a lower price than on-demand invocation, so a latency-tolerant bulk job costs less without any per-request code.",
    optionRationale: {
      a: "Batch inference takes the prompts as files in S3, runs them as one asynchronous job, and writes the results back to S3, which matches both the volume and the timeline.",
      b: "Provisioned Throughput reserves capacity for a committed term at an hourly rate, which is poor value for a one-off job that has no latency requirement.",
      c: "Calling the model once per article is exactly the per-request code the team wants to avoid, and it pays the full on-demand price.",
      d: "The Priority tier charges a premium for faster responses, the opposite of what a job nobody is waiting on needs.",
      e: "Streaming changes when the text arrives, not what each token costs, and it still means sending millions of requests.",
      f: "A knowledge base splits, embeds, and indexes documents for retrieval; it does not write a summary of each one.",
      g: "An always-on real-time endpoint bills for instances by the hour and is built for low-latency traffic, not a cheap offline job.",
      h: "An asynchronous batch job processes the whole collection from one request, and supported models are priced lower for batch than for on-demand calls.",
      i: "Larger models generally cost more per token, not less, so this would raise the bill.",
    },
    referenceUrl: "https://docs.aws.amazon.com/bedrock/latest/userguide/batch-inference.html",
    referenceLabel: "Process multiple prompts with batch inference",
    consoleUrl: "https://console.aws.amazon.com/bedrock/",
    consoleLabel: "Amazon Bedrock console",
  },
  {
    id: "aif-gen39",
    domain: "genai-fundamentals",
    text: "A German insurer's claims assistant calls a foundation model on Amazon Bedrock from the Europe (Frankfurt) Region, and at month end its requests are throttled. Policy says customer data may be processed only inside the EU, and the team does not want to pay for dedicated or reserved capacity. The model offers both an EU geographic and a global cross-Region inference profile. What should the team do?",
    options: [
      { id: "a", text: "Call the model through its EU geographic inference profile, so Amazon Bedrock can route requests to other EU Regions when Frankfurt is busy" },
      { id: "b", text: "Call the model through its global inference profile, which routes requests to commercial Regions worldwide" },
      { id: "c", text: "Buy Provisioned Throughput for the model with a one-month commitment to cover the month-end peaks" },
      { id: "d", text: "Move the application to a US Region, where the model has higher quotas and more spare capacity" },
      { id: "e", text: "Switch the claims assistant to batch inference so that month-end requests are queued and run later" },
      { id: "f", text: "Put Amazon CloudFront in front of Amazon Bedrock so that edge locations absorb the month-end peaks" },
      { id: "g", text: "Lower the temperature so that each request finishes faster and fewer requests are throttled" },
      { id: "h", text: "Replace the In-Region model ID with the EU inference profile ID, letting Bedrock spread requests across Regions inside the EU" },
      { id: "i", text: "Turn on Amazon S3 Cross-Region Replication for the prompts so that other EU Regions can serve them" },
    ],
    correctOptionIds: ["a", "h"],
    answerType: "single",
    explanation: "Cross-Region inference sends requests through an inference profile that can route each one to any of several Regions, which raises the throughput available during peaks with no extra routing charge. A geographic profile (such as US, EU, or APAC) keeps processing inside that geography, which suits data residency rules, while a global profile can route to supported commercial Regions anywhere and suits workloads with no residency constraint.",
    optionRationale: {
      a: "The EU geographic profile draws on capacity in several EU Regions, easing the throttling while keeping processing inside the EU.",
      b: "A global profile can send prompts to Regions outside the EU, which breaks the insurer's residency policy.",
      c: "Provisioned Throughput is dedicated capacity bought for a term, which the team has ruled out.",
      d: "Processing claims data in a US Region breaks the EU-only policy.",
      e: "Batch inference returns results asynchronously, which does not work for an interactive assistant.",
      f: "CloudFront caches and delivers web content; it adds no model capacity and cannot reuse unique generated answers.",
      g: "Temperature affects how varied the wording is, not how many requests the account can send.",
      h: "Using the EU profile ID in place of the Frankfurt model ID is how cross-Region inference is turned on, and the EU profile only routes to EU Regions.",
      i: "S3 replication copies stored objects between buckets; it adds no model capacity, and no other Region serves the model calls because of it.",
    },
    referenceUrl: "https://docs.aws.amazon.com/bedrock/latest/userguide/cross-region-inference.html",
    referenceLabel: "Route model inference requests across AWS Regions with cross-Region inference",
    consoleUrl: "https://console.aws.amazon.com/bedrock/",
    consoleLabel: "Amazon Bedrock console",
    cliExample: {
      description: "List the cross-Region inference profiles that can be called from Europe (Frankfurt); the prefix shows each profile's geography.",
      command: "aws bedrock list-inference-profiles --type-equals SYSTEM_DEFINED --region eu-central-1 --query 'inferenceProfileSummaries[].inferenceProfileId' --output text",
      sampleOutput: "eu.anthropic.claude-sonnet-4-5-20250929-v1:0\tglobal.anthropic.claude-sonnet-4-5-20250929-v1:0\teu.meta.llama3-2-3b-instruct-v1:0",
    },
  },
  {
    id: "aif-gen40",
    domain: "genai-fundamentals",
    text: "A media technology company hosts an open weights language model itself on Amazon EC2. Every month it fine-tunes the model on new data, and it serves the model to customers around the clock. It wants to use AI chips designed by AWS to lower the cost of both steps. Which plan fits?",
    options: [
      { id: "a", text: "Fine-tune on Trn instances powered by AWS Trainium, and serve on Inf2 instances powered by AWS Inferentia2" },
      { id: "b", text: "Fine-tune on Inf2 instances powered by AWS Inferentia2, and serve on Trn instances powered by AWS Trainium" },
      { id: "c", text: "Run both steps on AWS Graviton processors, the AWS chips built specifically for deep learning" },
      { id: "d", text: "Run both steps on AWS Inferentia, which AWS designed for training large models" },
      { id: "e", text: "Use the AWS Nitro System as the accelerator that runs the model" },
      { id: "f", text: "Move both steps to Amazon Bedrock on-demand inference, which runs on chips in the customer's own EC2 fleet" },
      { id: "g", text: "Fine-tune on Amazon Braket quantum processors and serve on AWS Inferentia" },
      { id: "h", text: "Run both the fine-tuning and the serving on AWS Trainium instances, since Trainium is built for cost-effective training and inference" },
    ],
    correctOptionIds: ["a", "h"],
    answerType: "single",
    explanation: "AWS designs its own accelerators for teams that host models themselves: AWS Trainium, in Trn instances, is built for high-performance, cost-effective training and also serves inference at scale, while AWS Inferentia, in Inf instances, is built only for low-cost, high-throughput inference. Both are programmed through the AWS Neuron SDK, which works with frameworks such as PyTorch.",
    optionRationale: {
      a: "Trainium is purpose-built for training and Inferentia2 for inference, so each step runs on the chip designed for it.",
      b: "Inferentia is an inference chip and is not designed for training, so the fine-tuning half of this plan is wrong.",
      c: "Graviton is a general-purpose Arm CPU for everyday workloads, not an accelerator for training or serving large models.",
      d: "Inferentia is designed for inference; training belongs on Trainium.",
      e: "The Nitro System provides virtualization, networking, and security for EC2 instances and does not accelerate models.",
      f: "Bedrock is serverless, so no chips run in the customer's account, and moving to Bedrock means giving up the self-hosting the company chose.",
      g: "Amazon Braket gives access to quantum computers for research and is not a way to fine-tune language models.",
      h: "AWS positions Trainium for both training and inference, so one Trainium fleet can cover the monthly fine-tuning and the continuous serving.",
    },
    referenceUrl: "https://aws.amazon.com/ai/machine-learning/trainium/",
    referenceLabel: "AWS Trainium",
    consoleUrl: "https://console.aws.amazon.com/ec2/",
    consoleLabel: "Amazon EC2 console",
  },
  {
    id: "aif-gen41",
    domain: "genai-fundamentals",
    text: "A travel company has fine-tuned a model on Amazon Bedrock with 20,000 past itinerary descriptions so that it writes in the house style. The finance lead now asks what owning that custom model will cost over the coming year. Which statement is accurate?",
    options: [
      { id: "a", text: "The custom model brings a training charge per token processed, a monthly fee to store it, and the cost of serving it" },
      { id: "b", text: "Fine-tuning on Amazon Bedrock has no training charge; only the Amazon S3 storage for the training data is billed" },
      { id: "c", text: "A fine-tuned model is billed only for the hours it spends training and is then free to invoke in production" },
      { id: "d", text: "Deleting the training data from Amazon S3 after the job ends removes every ongoing charge for the custom model" },
      { id: "e", text: "Fine-tuning is billed as a flat monthly subscription, whatever the size of the dataset or the number of epochs" },
      { id: "f", text: "Serving the custom model runs through Provisioned Throughput or, for supported models, an on-demand custom model deployment, and that inference is billed" },
      { id: "g", text: "Once fine-tuned, the model keeps learning from new descriptions automatically at no extra cost" },
      { id: "h", text: "Each call to the custom model is billed at one flat fee per request, however many tokens it reads or writes" },
      { id: "i", text: "Custom models are stored free of charge for as long as the account stays open" },
      { id: "j", text: "Retraining the custom model whenever the house style or the source data changes incurs the training charge again" },
    ],
    correctOptionIds: ["a", "f", "j"],
    answerType: "single",
    explanation: "A custom model on Amazon Bedrock brings three kinds of charge: training, billed by the tokens in the training data multiplied by the number of epochs; a monthly fee for storing the custom model; and inference, which runs through Provisioned Throughput or, for supported models, an on-demand custom model deployment. None of these ends when training finishes, and every retraining run to follow a new house style or new data is billed again, which is why teams weigh that total against the per-request token cost of prompting a base model instead.",
    optionRationale: {
      a: "Training, monthly storage, and serving are the three cost elements a custom model adds.",
      b: "Model customization is billed per token processed during training, in addition to any S3 storage.",
      c: "Inference on a custom model is charged too, through Provisioned Throughput hours or on-demand token pricing.",
      d: "The training data is only an input to the job; the stored custom model and its serving are billed whether or not that data is kept.",
      e: "The training charge scales with the tokens processed, so a larger dataset or more epochs cost more.",
      f: "A custom model has to be served through Provisioned Throughput or, where supported, an on-demand custom model deployment, and either way its inference is charged.",
      g: "A custom model is fixed once trained; new data only reaches it through another paid customization job.",
      h: "Custom model inference is billed by Provisioned Throughput hours or by tokens on an on-demand deployment, not by a flat fee per request.",
      i: "Custom model storage is a monthly charge for each model kept in the account.",
      j: "A fine-tuned model captures the data it was trained on, so changes to the style or source data mean paying for another training job.",
    },
    referenceUrl: "https://aws.amazon.com/bedrock/pricing/",
    referenceLabel: "Amazon Bedrock pricing",
    consoleUrl: "https://console.aws.amazon.com/bedrock/",
    consoleLabel: "Amazon Bedrock console",
  },
  {
    id: "aif-gen42",
    domain: "genai-fundamentals",
    text: "A five-person startup with no machine learning specialists wants to launch an AI writing assistant for its customers within six weeks, and wants spending to stay small until the product proves itself. Which TWO advantages of building on an AWS managed generative AI service, such as Amazon Bedrock, matter most for this business?",
    options: [
      { id: "a", text: "Faster time to market, because ready-trained models can be called through an API instead of being built first" },
      { id: "b", text: "A lower barrier to entry, because no machine learning specialists are needed to train, host, or scale models" },
      { id: "c", text: "Exclusive rights to the underlying foundation model, so competitors cannot launch a similar writing feature" },
      { id: "d", text: "Guaranteed accuracy for every generated answer, so the startup can skip testing and human review entirely" },
      { id: "e", text: "A fixed monthly price that stays the same however many customers end up using the assistant" },
      { id: "f", text: "Automatic compliance of the product with the regulations of every country the startup sells in" },
      { id: "g", text: "Ownership of the provider's model weights, which the startup can list on its balance sheet as an asset" },
      { id: "h", text: "Cost-effectiveness, because pay-as-you-go pricing needs no upfront investment and grows only with real usage" },
      { id: "i", text: "A choice of high-performing models from leading providers, so the team can pick one that fits its quality and budget goals" },
      { id: "j", text: "Free, unlimited model usage during the startup's first year of running the assistant in production" },
      { id: "k", text: "Full responsibility for the startup's customer data passing from the startup to AWS" },
    ],
    correctOptionIds: ["a", "b", "h", "i"],
    answerType: "multi",
    explanation: "Managed generative AI services make the technology accessible to small teams: pretrained models from several providers are ready behind an API, AWS runs the infrastructure, and usage-based pricing avoids upfront spending. Together these lower the barrier to entry, shorten time to market, and keep costs in line with adoption, while the startup still owns testing, compliance, and the handling of its customers' data.",
    optionRationale: {
      a: "Calling an existing model through an API removes months of model building and training from the launch plan.",
      b: "The service handles training, hosting, and scaling, so a team without ML specialists can still ship the feature.",
      c: "The same models are available to every customer of the service, so they give no exclusive rights.",
      d: "No service guarantees correct output; evaluation and review remain the startup's job.",
      e: "On-demand pricing is usage based, so the bill rises and falls with how much the assistant is used.",
      f: "Compliance of the product remains the startup's responsibility under the shared responsibility model.",
      g: "Using a model through the service grants no ownership of the provider's weights.",
      h: "Paying per use with no upfront commitment keeps costs small while the product is unproven and lets them grow with demand.",
      i: "A choice of models from several providers lets the startup trade quality against price to fit its goals instead of settling for one model.",
      j: "Model usage is billed; there is no unlimited free production tier.",
      k: "The customer keeps responsibility for its own data and how the application uses it.",
    },
    referenceUrl: "https://aws.amazon.com/bedrock/faqs/",
    referenceLabel: "Amazon Bedrock FAQs",
    consoleUrl: "https://console.aws.amazon.com/bedrock/",
    consoleLabel: "Amazon Bedrock console",
  },
  {
    id: "aif-gen43",
    domain: "genai-fundamentals",
    text: "An online retailer uses one model on Amazon Bedrock for two workloads: a shopping assistant that customers chat with during flash sales, where every second of delay loses sales, and an internal job that summarizes product reviews for a weekly report and can wait. Neither workload justifies reserving capacity. Which choice gives the assistant the fastest responses while cutting the cost of the review job?",
    options: [
      { id: "a", text: "Priority tier for the shopping assistant, and Flex tier for the review summaries" },
      { id: "b", text: "Flex tier for the shopping assistant, and Priority tier for the review summaries" },
      { id: "c", text: "Priority tier for both workloads, so that every request is answered as fast as possible" },
      { id: "d", text: "Flex tier for both workloads, to get the lower price on every request" },
      { id: "e", text: "Reserved tier for both workloads, with a three-month capacity reservation" },
      { id: "f", text: "Batch inference for the shopping assistant, and Standard tier for the review summaries" },
      { id: "g", text: "Provisioned Throughput for the shopping assistant, and Priority tier for the review summaries" },
      { id: "h", text: "Priority tier for the shopping assistant, and a batch inference job for the review summaries" },
      { id: "i", text: "Standard tier for the shopping assistant, and Priority tier for the review summaries" },
    ],
    correctOptionIds: ["a", "h"],
    answerType: "single",
    explanation: "Amazon Bedrock service tiers trade responsiveness against price per request: Priority charges a premium to serve requests ahead of Standard and Flex traffic, Standard is the default, Flex gives a discount in exchange for longer processing times, and Reserved dedicates capacity for a term. Latency-sensitive customer traffic belongs on Priority, while work that can wait saves money on Flex or as a discounted batch inference job.",
    optionRationale: {
      a: "Priority serves the customer-facing requests first, and Flex discounts the review job, which can tolerate slower processing.",
      b: "This reverses the trade-off, slowing the assistant and paying a premium for a job nobody is waiting on.",
      c: "Paying the Priority premium for the weekly review job raises its cost instead of cutting it.",
      d: "Flex accepts longer processing times, which would slow the assistant during flash sales.",
      e: "The Reserved tier requires a capacity reservation for a term, which neither workload justifies.",
      f: "Batch inference returns results asynchronously and cannot serve a live chat, and Standard gives the review job no discount.",
      g: "Provisioned Throughput is reserved capacity, which the retailer has ruled out, and Priority adds cost to the review job.",
      h: "Priority gives the assistant the fastest responses, and a batch job runs the weekly summaries at a lower price than on-demand.",
      i: "Standard gives the assistant no prioritization, and Priority adds cost where speed does not matter.",
    },
    referenceUrl: "https://docs.aws.amazon.com/bedrock/latest/userguide/service-tiers-inference.html",
    referenceLabel: "Service tiers for optimizing performance and cost",
    consoleUrl: "https://console.aws.amazon.com/bedrock/",
    consoleLabel: "Amazon Bedrock console",
  },
  {
    id: "aif-gen44",
    domain: "genai-fundamentals",
    text: "For a cost estimate, assume a model on Amazon Bedrock is priced at $3 per million input tokens and $15 per million output tokens. Each request to the new feature sends 3,000 input tokens and receives 1,000 output tokens. Which statement about the cost of one request is correct?",
    options: [
      { id: "a", text: "The 1,000 output tokens cost more than the 3,000 input tokens" },
      { id: "b", text: "Each request costs the same whatever the response length, because Amazon Bedrock bills per request" },
      { id: "c", text: "The input tokens make up most of the cost, because there are three times as many of them" },
      { id: "d", text: "Removing 1,000 tokens from the prompt saves exactly as much as removing 1,000 tokens from the response" },
      { id: "e", text: "Each request costs $0.060 in model charges" },
      { id: "f", text: "Each request costs $0.012 in model charges" },
      { id: "g", text: "Raising the temperature lowers the price charged for each output token" },
      { id: "h", text: "Capping responses at 500 tokens saves more per request than cutting 1,000 tokens from the prompt" },
      { id: "i", text: "Output tokens are not billed when generation ends at a stop sequence" },
      { id: "j", text: "Each request costs $0.024 in model charges" },
    ],
    correctOptionIds: ["a", "h", "j"],
    answerType: "single",
    explanation: "On-demand pricing counts input and output tokens separately, and on most models an output token costs more than an input token. Here the input costs 3,000 x $3 / 1,000,000 = $0.009 and the output 1,000 x $15 / 1,000,000 = $0.015, so the shorter output is the larger share of the $0.024 total. The main levers are shorter prompts and a lower maximum response length, and each output token removed saves five times as much as an input token.",
    optionRationale: {
      a: "The output costs $0.015 against $0.009 for the input, because each output token is priced five times higher.",
      b: "On-demand text models are billed per token, so a longer response costs more.",
      c: "The input is only $0.009 of the $0.024 total; the higher output price outweighs the larger input count.",
      d: "Removing 1,000 prompt tokens saves $0.003, while removing 1,000 response tokens saves $0.015.",
      e: "$0.060 would mean charging all 4,000 tokens at the output rate.",
      f: "$0.012 would mean charging all 4,000 tokens at the input rate.",
      g: "Temperature changes how tokens are sampled, not what each token costs.",
      h: "Halving the response saves 500 x $15 per million, or $0.0075, while trimming the prompt saves only 1,000 x $3 per million, or $0.003.",
      i: "Every generated output token is billed, however generation stops.",
      j: "Input at $0.009 plus output at $0.015 gives $0.024 per request.",
    },
    referenceUrl: "https://aws.amazon.com/bedrock/pricing/",
    referenceLabel: "Amazon Bedrock pricing",
    consoleUrl: "https://console.aws.amazon.com/bedrock/",
    consoleLabel: "Amazon Bedrock console",
  },
  {
    id: "aif-gen45",
    domain: "genai-fundamentals",
    text: "A startup in Kenya runs its application in the AWS Region closest to its customers and finds that the foundation model it has chosen is not offered In-Region there on Amazon Bedrock. Company policy allows prompts to be processed in other countries. How can the team use the model without moving the application?",
    options: [
      { id: "a", text: "Call the model through a global cross-Region inference profile, if the model's Regional availability lists one for that source Region" },
      { id: "b", text: "Call the model by its plain In-Region model ID, and Amazon Bedrock will forward the request to a Region that has it" },
      { id: "c", text: "Buy Provisioned Throughput in the current Region, which makes any model available there" },
      { id: "d", text: "Request a service quota increase for the model in the current Region" },
      { id: "e", text: "Ask AWS Support to install a copy of the model in that Region for the account" },
      { id: "f", text: "Wait for Amazon Bedrock to replicate the model into the Region automatically after the first failed request" },
      { id: "g", text: "Place Amazon CloudFront in front of Amazon Bedrock to cache the model at a nearby edge location" },
      { id: "h", text: "Send the model requests to the Amazon Bedrock endpoint in another Region where the model is offered In-Region" },
      { id: "i", text: "Switch the requests to the Flex service tier, which serves every model in every Region" },
    ],
    correctOptionIds: ["a", "h"],
    answerType: "single",
    explanation: "Model availability on Amazon Bedrock varies by Region, and the Regional availability page shows for each model whether it is offered In-Region, through a geographic cross-Region profile, or through a global profile. When a model is not offered In-Region, the application can call another Region's endpoint or use a cross-Region profile, and both options mean prompts are processed outside the source Region. That is acceptable only because this policy allows it; a workload that must stay in one Region or geography needs an In-Region model or a geographic profile that stays inside the boundary.",
    optionRationale: {
      a: "A global profile routes requests from the source Region to Regions where the model runs, and the policy permits processing abroad.",
      b: "An In-Region request is processed only in the Region specified, so it cannot reach a model that is not offered there.",
      c: "Provisioned Throughput buys capacity for a model already offered in a Region; it does not bring a new model to it.",
      d: "A quota raises limits on a model that is available; it cannot make an unavailable model appear.",
      e: "AWS decides where each model is offered; Support does not install models in a Region for one account.",
      f: "Bedrock does not copy models into a Region in response to failed calls.",
      g: "CloudFront caches web content at the edge and cannot host or run a foundation model.",
      h: "An application can call the Bedrock endpoint of any Region where the model is offered, and the policy allows the prompts to be processed there.",
      i: "Service tiers change priority and price for a model in a Region, not where the model is offered.",
    },
    referenceUrl: "https://docs.aws.amazon.com/bedrock/latest/userguide/models-region-compatibility.html",
    referenceLabel: "Regional availability by models (Amazon Bedrock)",
    consoleUrl: "https://console.aws.amazon.com/bedrock/",
    consoleLabel: "Amazon Bedrock console",
  },
  {
    id: "aif-gen46",
    domain: "genai-fundamentals",
    text: "A US health insurer's risk committee is reviewing a plan to build a claims assistant on Amazon Bedrock. Which TWO statements describe security and compliance benefits the insurer gets from building on AWS infrastructure?",
    options: [
      { id: "a", text: "Amazon Bedrock is HIPAA eligible, so it can be used in workloads that handle protected health information" },
      { id: "b", text: "Amazon Bedrock is in scope for AWS's SOC reports and ISO certifications, so its controls are independently audited" },
      { id: "c", text: "Because Amazon Bedrock is HIPAA eligible, the claims assistant is automatically HIPAA compliant with no further work" },
      { id: "d", text: "AWS's SOC and ISO audits also certify the insurer's own application, so it needs no controls of its own" },
      { id: "e", text: "Amazon Bedrock encrypts stored data only for requests that are sent on the Priority service tier" },
      { id: "f", text: "AWS reviews and decides which of the insurer's data may be placed in each prompt" },
      { id: "g", text: "Amazon Bedrock guarantees that protected health information can never appear in a model response" },
      { id: "h", text: "Data is encrypted in transit and at rest, and the insurer can use its own AWS KMS keys for resources such as custom models" },
      { id: "i", text: "The encryption keys for the insurer's data are held and managed by the third-party model providers" },
      { id: "j", text: "AWS secures the facilities, hardware, and software that run the service, and the insurer inherits those controls" },
      { id: "k", text: "A managed service removes the need to control which of the insurer's staff and systems can invoke models" },
    ],
    correctOptionIds: ["a", "b", "h", "j"],
    answerType: "multi",
    explanation: "Building on AWS gives a regulated company a secured and independently audited base: Amazon Bedrock is in scope for SOC and ISO, is HIPAA eligible, encrypts data in transit and at rest with the option of customer managed AWS KMS keys, and AWS secures the infrastructure that runs the service. Those benefits do not make the application compliant by themselves; under the shared responsibility model the insurer still controls access, decides what data goes into prompts, and configures safeguards.",
    optionRationale: {
      a: "HIPAA eligibility means Bedrock can be part of a workload that processes protected health information, provided the customer meets its own obligations.",
      b: "Bedrock is covered by AWS's SOC reports and ISO certifications, giving the insurer third-party assurance about the service's controls.",
      c: "Eligibility only allows the service to be used; the insurer must still build and operate the application in a compliant way.",
      d: "AWS's audits cover AWS's controls, not the insurer's application, which needs its own controls and assurance.",
      e: "Bedrock encrypts data at rest and in transit regardless of the service tier used.",
      f: "Choosing and classifying the data that goes into prompts is the customer's responsibility.",
      g: "No service can guarantee what a model will output; the insurer has to add safeguards such as guardrails and review.",
      h: "Bedrock encrypts data in transit and at rest, and customer managed KMS keys can be used for resources such as custom models, model evaluation jobs, and knowledge base data.",
      i: "Model providers have no access to customer data or its keys; encryption is handled by AWS and, optionally, the customer's own KMS keys.",
      j: "Under the shared responsibility model AWS secures the infrastructure behind a managed service, and the customer inherits those controls.",
      k: "Access to models is controlled by the customer through IAM, which remains the insurer's job.",
    },
    referenceUrl: "https://docs.aws.amazon.com/prescriptive-guidance/latest/security-reference-architecture-generative-ai/gen-ai-sra.html",
    referenceLabel: "AWS Security Reference Architecture (AWS SRA) for AI",
    consoleUrl: "https://console.aws.amazon.com/bedrock/",
    consoleLabel: "Amazon Bedrock console",
  },
  {
    id: "aif-gen47",
    domain: "genai-fundamentals",
    text: "A restaurant chain's operations managers do not write SQL. They want to type questions such as \"which locations had the biggest drop in weekend sales last month?\" about their sales data and get charts back. Head office wants a generated summary of the key insights on each weekly dashboard, and analysts want to build new visuals by describing them. Which AWS capability fits?",
    options: [
      { id: "a", text: "Generative BI in Amazon Quick Sight, the business intelligence capability of Amazon Quick" },
      { id: "b", text: "Amazon Athena, which runs SQL queries directly against data stored in Amazon S3" },
      { id: "c", text: "Amazon Redshift, a cloud data warehouse for large-scale SQL analytics" },
      { id: "d", text: "AWS Glue DataBrew, which cleans and normalizes data through a visual interface" },
      { id: "e", text: "Amazon Comprehend, which finds sentiment, entities, and key phrases in text" },
      { id: "f", text: "Amazon CloudWatch dashboards, which chart metrics published by AWS resources" },
      { id: "g", text: "Amazon Lex, which builds chatbots from intents and slots" },
      { id: "h", text: "Amazon Quick chat over Quick Sight data, which answers questions with visuals and summarizes dashboards" },
      { id: "i", text: "AWS Glue Data Catalog, which stores table definitions and schemas" },
    ],
    correctOptionIds: ["a", "h"],
    answerType: "single",
    explanation: "Generative BI in Amazon Quick Sight, the business intelligence part of Amazon Quick, lets business users ask questions of their data in plain language and get multi-visual answers, generates executive summaries and data stories from dashboards, and lets authors build visuals and calculations by describing them. It is built on Amazon Bedrock, so no one on the team writes SQL or manages a model.",
    optionRationale: {
      a: "Quick Sight's generative BI answers natural-language questions with charts, writes executive summaries of dashboards, and builds visuals from descriptions.",
      b: "Athena needs SQL, which the managers do not write, and it produces no summaries or dashboards.",
      c: "Redshift stores and queries data at scale but is not a natural-language BI tool.",
      d: "DataBrew cleans and prepares data visually; it does not answer business questions or summarize dashboards.",
      e: "Comprehend analyzes free text and does not chart or summarize sales data.",
      f: "CloudWatch dashboards show operational metrics for AWS resources, not business sales analysis.",
      g: "Lex builds scripted conversational bots and does not analyze or chart a dataset.",
      h: "Amazon Quick chat works with Quick Sight data to answer questions with visuals, author new visuals, and produce executive summaries.",
      i: "The Data Catalog stores table metadata and has no question-answering or dashboard features.",
    },
    referenceUrl: "https://docs.aws.amazon.com/quick/latest/userguide/quicksight-gen-bi.html",
    referenceLabel: "Generative BI with Quick Sight",
  },
  {
    id: "aif-gen48",
    domain: "genai-fundamentals",
    text: "A biotech startup already builds on Amazon Bedrock with the Converse API and Amazon Bedrock Guardrails. It now needs a specialized protein research model from a smaller provider that is not one of Bedrock's serverless models, and the provider licenses the model without publishing its weights. The team wants to find the model in the Bedrock console, deploy it to a managed endpoint, and call it through Bedrock's APIs. Which capability fits?",
    options: [
      { id: "a", text: "Amazon Bedrock Marketplace" },
      { id: "b", text: "Amazon Bedrock Custom Model Import, which brings in model weights from Amazon S3" },
      { id: "c", text: "Provisioned Throughput for one of the existing serverless models in Amazon Bedrock" },
      { id: "d", text: "A cross-Region inference profile, which adds other providers' models to the account" },
      { id: "e", text: "Amazon Bedrock Knowledge Bases, which connects models to company documents" },
      { id: "f", text: "PartyRock, the Amazon Bedrock playground for building small apps" },
      { id: "g", text: "Amazon Comprehend Medical, which extracts medical entities from clinical text" },
      { id: "h", text: "Subscribe to the model in the Bedrock model catalog and deploy it through Amazon Bedrock Marketplace" },
      { id: "i", text: "AWS Data Exchange, which licenses third-party datasets" },
    ],
    correctOptionIds: ["a", "h"],
    answerType: "single",
    explanation: "Amazon Bedrock Marketplace offers over 100 popular, emerging, and specialized models alongside the serverless models in Amazon Bedrock. The team finds a model in the Bedrock model catalog, subscribes to it, and deploys it to a managed endpoint with the instance type and count it chooses, then calls it through Bedrock's APIs, and models compatible with the Converse API work with Bedrock tools such as Guardrails and Knowledge Bases. Unlike serverless models billed per token, the endpoint's instances are billed while it runs, along with any fee the provider sets.",
    optionRationale: {
      a: "Bedrock Marketplace is how specialized models outside the serverless catalog are discovered, deployed to managed endpoints, and called through Bedrock's APIs.",
      b: "Custom Model Import needs the model weights, which this provider does not publish.",
      c: "Provisioned Throughput adds capacity to a model already in the catalog; it cannot add a new provider's model.",
      d: "Inference profiles route requests for existing models across Regions and add no new models.",
      e: "Knowledge Bases retrieves the company's own documents for RAG and does not supply models.",
      f: "PartyRock is a public playground for prototyping apps and cannot deploy a licensed third-party model.",
      g: "Comprehend Medical extracts medical information from clinical text and is not a protein research model.",
      h: "Subscribing in the Bedrock model catalog and deploying through Bedrock Marketplace gives a managed endpoint that is invoked through Bedrock's APIs.",
      i: "Data Exchange licenses third-party data, not foundation models for inference.",
    },
    referenceUrl: "https://docs.aws.amazon.com/bedrock/latest/userguide/amazon-bedrock-marketplace.html",
    referenceLabel: "Amazon Bedrock Marketplace",
    consoleUrl: "https://console.aws.amazon.com/bedrock/",
    consoleLabel: "Amazon Bedrock console",
  },
];
